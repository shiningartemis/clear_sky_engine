# AI 角色扮演生活模拟游戏：技术选型文档

> 文档状态：经产品与架构确认后的技术基线
> 日期：2026-07-11
> 目标平台：Windows 11 x64
> 设计原则：使用能够完整交付产品的最小技术集合；必需能力不省略，无当前用途的实体不加入。

## 1. 决策摘要

采用模块化单体：

- 后端：Python、FastAPI、Pydantic、SQLAlchemy、Alembic、HTTPX；
- 数据库：SQLite；
- 前端：React、TypeScript、Vite、CSS Modules；
- 2D 游戏：Phaser 4；
- AI：DeepSeek 官方 API 与通用 OpenAI-compatible API；
- 打包：PyInstaller onedir；
- 运行：一个 Python 进程、一个 SQLite 数据库、一个系统浏览器页面。

项目内实现一个小型分阶段流水线执行器。第一版任务由代码定义，但执行器从一开始支持“阶段顺序执行、阶段内并行任务”。不引入工作流引擎、消息队列、第三方多 Agent 框架或向量检索。

## 2. 运行时基线

| 项目 | 基线 |
|---|---|
| 操作系统 | Windows 11 x64 |
| Python | 3.14.6 |
| Python 项目工具 | uv |
| Node.js | 24.18.0 LTS |
| npm | 随 Node 24，初始化时锁定 |
| SQLite | 项目锁定的 Python 3.14.6 运行时自带版本 |
| 浏览器 | 当前受支持的 Edge、Chrome、Firefox |

选择依据：

- Python 3.14.6 是 2026-06-10 发布的 3.14 维护版；
- PyInstaller 从 6.15 起支持 Python 3.14；
- Node 24 当前处于 LTS，适合新项目；
- SQLite 跟随项目锁定的 Python 3.14.6 运行时自带版本，不单独替换或捆绑 SQLite DLL；启动、检查和打包记录实际 `sqlite3.sqlite_version`。

版本管理规则：

1. 根目录使用 .python-version 固定 Python 3.14.6；
2. uv.lock 与 package-lock.json 必须提交；
3. package.json 使用 engines 约束 Node 24，并固定 packageManager；
4. 构建脚本检查实际 Python、Node、npm 和 SQLite 版本；
5. 生产依赖必须由锁文件确定，不使用浮动安装；
6. 更新版本时同时更新本文、锁文件、测试和构建验证；
7. FastAPI 等快速迭代库固定到实际测试通过的版本，不把“永远最新”当作版本策略。

## 3. 总体架构

    系统默认浏览器
    └── React
        ├── 页面、表单、时间线和故事
        ├── 轮次进度、取消和重试
        └── Phaser 4 地图画布
                 │
                 │ REST + NDJSON
                 ▼
    FastAPI 模块化单体
    ├── provider
    ├── workflow
    ├── world
    ├── character
    ├── attribute
    ├── story
    ├── game
    └── db
        │              │
        ▼              ▼
    AI Provider      SQLite

生产环境只有：

- 一个 Python 进程；
- 一个应用级 HTTPX AsyncClient；
- 一个 SQLite 数据库；
- 一个由 FastAPI 提供的前端静态站点；
- 一个系统默认浏览器页面。

Node.js 只用于开发和构建，不进入生产运行时。

## 4. 后端技术

### 4.1 运行依赖

| 技术 | 用途 |
|---|---|
| FastAPI | REST、OpenAPI、静态资源、NDJSON |
| Uvicorn | 本地 ASGI 服务 |
| Pydantic 2 | API DTO、AI 输出、属性命令和配置校验 |
| SQLAlchemy 2.0 | SQLite ORM、连接和短事务 |
| Alembic 1.x | 数据库迁移 |
| HTTPX | 异步 AI API、连接池和流式响应 |

不使用 Tenacity。轮次任务需要显式处理取消、任务状态、输入快照和输出是否已产生，项目内小型重试函数更清晰。

### 4.2 开发依赖

- pytest；
- pytest-asyncio；
- respx；
- Ruff；
- Pyright；
- PyInstaller。

### 4.3 数据库访问方式

使用 SQLAlchemy 同步 Session 和标准 SQLite 驱动。

理由：

- 数据库操作均为本地短事务；
- 等待 AI 时不持有连接或事务；
- 单用户、单进程场景不需要 aiosqlite 带来的额外抽象；
- AI 并发由 asyncio 管理，与数据库并发无关。

API 路由只接收输入、调用 Service 并转换 HTTP 响应。业务逻辑不得进入路由。

## 5. 前端与游戏技术

### 5.1 前端

| 技术 | 用途 |
|---|---|
| React 19.2 | 页面和交互 UI |
| TypeScript | 类型安全 |
| React Router | 页面路由 |
| Vite | 开发与生产构建 |
| 原生 Fetch | REST 与 NDJSON |
| CSS Modules | 样式隔离 |
| openapi-typescript | 从 OpenAPI 生成前端 API 类型 |
| Biome | 格式化和静态检查 |

状态优先级：

1. 组件本地 state；
2. useReducer；
3. 少量 Context。

不加入 Redux、Zustand、Axios、Tailwind、ESLint、Prettier或大型 UI 组件库。

### 5.2 Phaser 4

Phaser 负责：

- Tilemap 和场景；
- 主角与 NPC 精灵；
- 键盘或点击移动；
- Camera；
- 边界与障碍碰撞；
- 位置点和场景切换；
- 当前地点的角色可见性。

Phaser 不负责：

- 时间推进；
- AI 请求；
- 角色属性和记忆；
- 数据库访问；
- 世界事实裁决；
- Provider 与模型配置。

React 与 Phaser 通过项目内类型化 GameBridge 通信，不引入事件总线库。

### 5.3 第一版地图实现

场景固定为：

| scene_id | 显示名称 | 类型 | 背景文件 |
|---|---|---|---|
| world_map | 总地图 | 导航总图 | 总地图.jpg |
| home | 家 | 地点 | 家.jpg |
| park | 地下城 | 地点 | 地下城.jpg |
| mall | 商店街 | 地点 | 商店街.jpg |
| bar | 冒险者工会 | 地点 | 冒险者工会.jpg |
| love_hotel | 旅店 | 地点 | 旅店.jpg |
| school | 学校 | 地点 | 学校.jpg |

资源规则：

- 7 张占位图均为 1920×1080、JPG、16:9；
- 首次启动时，把缺失的默认占位图复制到 LocalAppData 的可编辑内容目录；
- 已存在的同名地图图片绝不被升级或启动流程覆盖；
- FastAPI 从可编辑内容目录提供地图图片；
- map_manifest.json 使用稳定 ASCII scene_id 关联中文文件名、地点顺序和总地图锚点；
- 图片不进入 SQLite；
- 地图静态响应使用 Cache-Control: no-cache，使同名文件替换后在刷新页面时重新验证；
- 文件缺失、损坏或比例不正确时记录可理解错误，并回退到内置空白占位图。

渲染结构：

1. Phaser Canvas 填满游戏视口；
2. 当前地图 JPG 作为最底层背景；
3. 主角、NPC、地点锚点和必要交互提示由 Phaser 绘制；
4. 时间、行动输入、故事、进度及“地图”按钮由 React 以绝对定位覆盖在 Canvas 上；
5. 16:9 视口完整贴合，其他比例使用 cover 居中裁切；
6. 所有关键锚点和浮动 UI 位于 16:9 安全区域。

总地图输入状态机：

- Left/Right 将目标切换到相邻地点，并播放主角小型立绘的移动动画；
- 第一次点击地点会选择该地点并移动主角；
- 主角到达已选地点后，再次点击或按 Enter 执行 enter_location；
- enter_location 切换到对应地点场景；
- 地点场景左上角的 React“地图”按钮执行 return_to_world_map；
- move、select_location、enter_location、return_to_world_map 都不推进故事时间。

地点地图只在玩家提交文字意图时创建 turn_run。进入地点后，后端根据该地点的 location_id 返回本轮可见 NPC，前端不得自行推断角色是否同处一地。

## 6. 后端模块边界

建议按业务能力组织：

    backend/src/app/
    ├── main.py
    ├── launcher.py
    ├── config.py
    ├── api/
    ├── ai/
    │   ├── models.py
    │   ├── errors.py
    │   ├── provider.py
    │   ├── registry.py
    │   └── deepseek.py
    ├── workflow/
    │   ├── models.py
    │   ├── definitions.py
    │   ├── runner.py
    │   └── context.py
    ├── world/
    ├── character/
    ├── attribute/
    ├── story/
    ├── game/
    ├── db/
    └── static/

规则：

- 不创建 domain/application/infrastructure/adapter 四层目录；
- Service 只有一个实现时不抽取接口；
- Store 直接使用 SQLAlchemy Session；
- 只有 Provider 和 Skill 确实存在多个实现，因此为它们定义稳定协议；
- 依赖通过普通构造函数传入，不使用依赖注入框架。

## 7. AI Provider 设计

### 7.1 Provider 层

第一版包含：

- OpenAICompatibleProvider；
- DeepSeekProvider，复用兼容协议，只覆盖 DeepSeek 专有能力；
- ProviderRegistry。

ProviderRegistry 只完成 provider_type 到无业务状态 Provider 实例的映射。

所有 Provider 共享应用级 HTTPX AsyncClient。禁止每次请求创建新客户端。

### 7.2 DeepSeek

首发模型：

- deepseek-v4-pro；
- deepseek-v4-flash。

支持：

- POST /chat/completions；
- 普通和流式输出；
- reasoning_content；
- thinking 模式；
- JSON Output；
- Tool Calls；
- token usage；
- DeepSeek finish_reason；
- SSE keep-alive 注释和空行；
- 官方错误响应映射。

旧模型别名 deepseek-chat 和 deepseek-reasoner 不作为产品默认值。

### 7.3 统一请求模型

统一请求至少包括：

- model_id；
- messages；
- temperature；
- max_output_tokens；
- thinking 配置；
- tools；
- response_format；
- provider_options。

统一响应至少包括：

- 文本；
- reasoning 内容；
- tool calls；
- finish reason；
- usage；
- provider_id；
- model_id。

provider_options 只作为厂商专有参数逃生口，不能把每个厂商字段塞进公共 DTO。

### 7.4 模型选择与失败

- 每个工作流任务必须人工选择一个模型；
- 不提供自动模型选择；
- 不提供模型 fallback；
- 临时网络错误可以在没有产生有效输出前有限重试；
- 任务持续失败时暂停本轮，由用户重试、取消或修改配置后重新开始。

## 8. 分阶段流水线

### 8.1 第一版能力

执行器支持：

- 阶段按定义顺序运行；
- 同一阶段的任务并行运行；
- 存在且仅存在一个不可删除的主 AI 任务；
- 每个任务有稳定 ID；
- 每个任务指定模型、参数、超时、提示词构造器、上下文选择器和输出 Schema；
- asyncio.TaskGroup 组织并行任务；
- asyncio.Semaphore 控制调用并发；
- cooperative cancellation；
- 输入哈希和任务结果复用。

第一版任务定义放在 workflow/definitions.py，不建设工作流 CRUD 表和配置页面。任务定义仍从数据库读取人工选择的模型与参数；缺少模型选择时拒绝启动轮次。

### 8.2 不实现

- 任意 DAG；
- 用户循环；
- 条件脚本；
- Python、JavaScript 或 Shell 节点；
- Celery、Redis 和外部队列；
- 第三方 Agent 编排框架。

### 8.3 未来演进

当用户需要编辑工作流时：

1. 将现有代码定义序列化为阶段与任务配置；
2. 增加数据库表和 CRUD；
3. 执行器保持不变；
4. 仍只支持顺序阶段和阶段内并行。

## 9. 一轮执行与一致性

### 9.1 创建运行

POST 创建 turn_run，立即返回 run_id。系统记录：

- world_id；
- branch_id；
- 当前世界版本；
- 当前天数和时间段；
- 玩家意图；
- 全部角色位置；
- 角色配置和属性；
- 最近 5 轮故事；
- 任务定义版本；
- 输入快照及哈希。

保存输入快照后结束事务。

### 9.2 执行

- 后台 asyncio Task 执行流水线；
- 每个任务只读取自己的上下文切片；
- 任务状态和中间结果持久化；
- 浏览器通过 NDJSON 获得进度；
- 断开页面后运行状态仍可通过 run_id 查询。

### 9.3 最终提交

在一个短事务中：

1. 检查 world 与 branch 仍处于输入快照版本；
2. 验证故事结构；
3. 验证信息边界；
4. 执行属性和世界状态命令；
5. 写入不可变 turn；
6. 写入角色视角、事件和状态变化；
7. 保存 state_after_json；
8. 更新分支当前状态与版本；
9. 推进时间；
10. 标记 turn_run 成功。

任何一步失败都回滚整个最终事务。

### 9.4 取消与重试

- 取消将运行标记为 cancelling，并通知所有未完成任务；
- 取消完成后状态为 cancelled；
- cancelled、failed、interrupted 不产生 turn；
- 重试使用原始输入快照；
- 输入未变的成功任务结果可以复用；
- 应用重启时把 running/cancelling 记录改为 interrupted；
- 第一版全应用同时只允许一个活动 turn_run。

## 10. 时间、位置与信息边界

### 10.1 时间

使用枚举：

- morning；
- midday；
- evening；
- night。

advance_time 是纯函数，night 后 day 加一并回到 morning。

当前星期由 world.start_weekday 与 day 推导，不单独维护一个可能漂移的可写星期字段。位置规则使用推导结果判断 weekday_mask。

### 10.2 位置

地图和位置目录由开发者 JSON 定义，使用稳定 location_id。

NPC 位置规则由数据库保存，程序在轮次开始前计算角色位置。AI 只能读取位置事实，不能任意更改地图或让角色瞬移。

### 10.3 信息隔离

每个 AI 任务使用 ContextBuilder。ContextBuilder 根据：

- 目标角色；
- 参与事件；
- 所在位置；
- 最近 5 轮角色可知故事；
- 明确被告知的事实；
- 世界公开事实；

生成最小上下文。

全局角色纪事只提供给玩家和被授权的全局分析任务，不进入普通角色记忆。

## 11. 属性与 Skill

### 11.1 属性值

属性值以 JSON 保存，但必须由 data_type 决定其合法类型：

- number；
- string；
- boolean；
- enum。

不得把所有值当成字符串处理。

### 11.2 状态命令

通用命令：

- replace；
- increment；
- decrement。

命令包含：

- character_id；
- attribute_key；
- operation；
- value 或 amount；
- reason；
- source_event_id；
- expected_version。

程序检查属性存在、操作允许、类型、上下限和版本后执行。

### 11.3 Skill

Skill 是注册的受控业务能力，例如：

- 购买；
- 战斗伤害；
- 关系变化；
- 获取当前世界状态。

Skill 可以读取必要状态并返回结构化结果，但不能绕过统一命令层直接提交任意 SQL。

第一版不向 AI 提供：

- 任意 Shell；
- 任意文件写入或删除；
- 任意 SQL；
- 动态下载和执行代码。

## 12. 数据库设计

### 12.1 全局表

#### ai_provider

- id；
- name；
- provider_type；
- base_url；
- api_key；
- enabled；
- extra_json；
- created_at；
- updated_at。

API Key 明文保存，但查询 API 只返回 has_api_key。

#### ai_model

- id；
- provider_id；
- display_name；
- remote_model；
- capabilities_json；
- defaults_json；
- enabled；
- created_at；
- updated_at。

#### app_setting

- key；
- value_json；
- updated_at。

#### asset_catalog

可由静态清单加载，不强制建立数据库表。若需要在 UI 中筛选，保存稳定 asset_id、类型和资源路径的只读索引；资产文件本身不进入 SQLite。

第一版地图目录由 map_manifest.json 管理，不建立可编辑地图数据库表。

### 12.2 世界与分支

#### world

- id；
- name；
- active_branch_id；
- start_weekday；
- created_at；
- updated_at；
- last_played_at。

#### world_branch

- id；
- world_id；
- name；
- parent_branch_id；
- fork_turn_id；
- head_turn_id；
- day；
- time_slot；
- state_version；
- created_at；
- updated_at。

分支中的角色配置和当前状态独立。创建分支时，从目标 turn 的 state_after_json 物化新的分支状态。

#### world_snapshot

- id；
- world_id；
- branch_id；
- turn_id；
- name；
- created_at。

快照只引用成功 turn。

### 12.3 分支当前状态

#### character

- id；
- world_id；
- branch_id；
- kind：player 或 npc；
- name；
- persona；
- system_prompt；
- world_book；
- asset_id；
- enabled；
- version；
- created_at；
- updated_at。

约束：

- 每个 branch 只能有一个 kind=player；
- player 不允许删除；
- 所有外键同时校验 world_id 与 branch_id。

#### character_location_rule

- id；
- world_id；
- branch_id；
- character_id；
- weekday_mask；
- time_slot；
- mode：fixed 或 random；
- priority；
- enabled。

#### character_location_candidate

- rule_id；
- location_id；
- weight。

#### attribute_template

- id；
- world_id；
- branch_id；
- key；
- name；
- data_type；
- default_value_json；
- min_value；
- max_value；
- enum_options_json；
- default_update_rule；
- created_at；
- updated_at。

#### character_attribute

- id；
- world_id；
- branch_id；
- character_id；
- template_id，可为空；
- key；
- name；
- data_type；
- initial_value_json；
- current_value_json；
- min_value；
- max_value；
- enum_options_json；
- allowed_operations_json；
- update_rule；
- version；
- created_at；
- updated_at。

模板被引用时，将有效配置保存到角色属性实例，避免模板变化静默改变历史状态。

#### workflow_task_setting

- id；
- world_id；
- branch_id；
- task_key；
- model_id；
- model_parameters_json；
- timeout_seconds；
- enabled；
- updated_at。

task_key 必须对应代码中存在的任务定义。主 AI 任务不能禁用，所有启用任务必须选择 model_id。

### 12.4 轮次运行

#### turn_run

- id；
- world_id；
- branch_id；
- status；
- player_intent；
- input_state_version；
- input_snapshot_json；
- input_hash；
- workflow_version；
- error_type；
- error_message；
- started_at；
- finished_at。

#### turn_task_run

- id；
- turn_run_id；
- task_key；
- stage_index；
- status；
- model_id；
- input_hash；
- output_json；
- error_type；
- started_at；
- finished_at。

### 12.5 成功轮次

#### turn

- id；
- world_id；
- branch_id；
- parent_turn_id；
- turn_run_id；
- day；
- time_slot；
- player_intent；
- objective_events_json；
- state_after_json；
- created_at。

#### turn_story

- id；
- turn_id；
- perspective_type：player 或 character；
- character_id，可为空；
- content；
- sort_order。

#### turn_event

- id；
- turn_id；
- location_id；
- event_type；
- fact_json；
- created_at。

#### turn_event_participant

- event_id；
- character_id；
- knowledge_level；
- perspective_notes。

#### state_change

- id；
- turn_id；
- event_id，可为空；
- character_id；
- attribute_key；
- operation；
- old_value_json；
- operand_json；
- new_value_json；
- reason。

每轮故事、事件、状态变化和 state_after_json 在同一数据库事务中完成一次逻辑保存。

## 13. SQLite 配置

每个连接启用：

    PRAGMA foreign_keys = ON;
    PRAGMA journal_mode = WAL;
    PRAGMA synchronous = FULL;
    PRAGMA busy_timeout = 5000;

规则：

- 等待 AI 时不得保持事务；
- 所有 schema 变更通过 Alembic；
- 启动时执行迁移前先备份数据库；
- 数据库文件只写入 LocalAppData；
- 图片、音频、地图和精灵不得以 BLOB 保存；
- 构建与启动记录并检查 `sqlite3.sqlite_version`；
- 生产包必须使用 Python 3.14.6 自带 SQLite，不单独替换运行时 DLL。

## 14. API 设计

至少包括：

### 全局配置

- /api/providers；
- /api/models；
- /api/app-settings；
- /api/health；
- /api/app/shutdown。

### 世界

- /api/worlds；
- /api/worlds/{world_id}/branches；
- /api/worlds/{world_id}/snapshots；
- /api/worlds/{world_id}/timeline。

### 角色与属性

- /api/worlds/{world_id}/branches/{branch_id}/characters；
- /api/worlds/{world_id}/branches/{branch_id}/attribute-templates；
- /api/worlds/{world_id}/branches/{branch_id}/location-rules。

### 轮次

- POST /api/worlds/{world_id}/turn-runs；
- GET /api/turn-runs/{run_id}；
- GET /api/turn-runs/{run_id}/stream；
- POST /api/turn-runs/{run_id}/cancel；
- POST /api/turn-runs/{run_id}/retry。

stream 使用 application/x-ndjson，每行一个完整 JSON 事件。

## 15. 本地安全边界

- Uvicorn 只绑定 127.0.0.1；
- 不启用公网或局域网访问；
- 生产环境不启用宽松 CORS；
- 校验 Host 只能是当前 loopback 地址和端口；
- 修改型 API 校验同源 Origin；
- shutdown 等敏感操作使用启动期随机令牌；
- API Key 不返回前端、不进入日志、不进入异常；
- API Key 可以替换或清空；
- 数据库备份包含明文 API Key，UI 必须提示风险。

这些措施不是用户登录系统，不引入 JWT、OAuth 或权限模型。

## 16. 打包与目录

第一版使用 PyInstaller onedir。

启动流程：

1. 确定 LocalAppData 数据目录；
2. 初始化日志；
3. 检查并备份数据库；
4. 执行 Alembic upgrade head；
5. 恢复中断的 turn_run 状态；
6. 选择空闲端口；
7. 启动 Uvicorn；
8. 打开默认浏览器。

可写目录：

    %LOCALAPPDATA%\ClearSkyEngine\
    ├── data\app.db
    ├── content\
    │   └── maps\
    │       ├── 总地图.jpg
    │       ├── 家.jpg
    │       ├── 地下城.jpg
    │       ├── 商店街.jpg
    │       ├── 冒险者工会.jpg
    │       ├── 旅店.jpg
    │       └── 学校.jpg
    ├── logs\
    └── backups\

前端构建产物复制到 backend/src/app/static，生产环境不启动 Node.js。

首次启动只复制缺失的占位地图；用户或开发者替换的同名图片必须保留。替换后刷新浏览器或重启应用即可生效，不提供运行中热更新。

## 17. 测试策略

### 17.1 后端

必须覆盖：

- OpenAI-compatible 请求、响应和流事件；
- DeepSeek thinking、JSON、Tool Calls、keep-alive 和错误映射；
- 阶段顺序；
- 阶段内并行；
- 并发上限；
- 取消、失败、中断和重试；
- 输入快照与结果复用；
- 时间推进；
- NPC 位置规则；
- 角色信息隔离；
- 属性类型、上下限和版本校验；
- 轮次原子提交；
- 世界隔离；
- 快照创建和分支物化；
- Alembic 从空库升级；
- API Key 不出现在查询与日志中。

真实付费 API 不进入自动测试，使用 respx 模拟。

### 17.2 前端

Vitest 与 React Testing Library 覆盖：

- API 转换；
- NDJSON 解析；
- 轮次进度；
- 取消和重试状态；
- 角色与属性表单；
- 时间线和分支；
- GameBridge；
- 地图清单解析；
- 总地图 Left/Right、鼠标选择、Enter 进入状态机；
- 地点地图返回总地图；
- 位置规则纯函数。

### 17.3 端到端

Playwright 覆盖少量关键路径：

1. 启动应用并打开世界列表；
2. 创建世界和角色；
3. 在总地图使用键盘和鼠标选择地点并进入；
4. 从地点地图返回总地图；
5. 替换同名 JPG 后刷新页面并显示新背景；
6. 进入地点地图并移动；
7. 提交行动并查看模拟进度；
8. 完成后显示两个故事模块；
9. 取消轮次且时间不推进；
10. 创建快照并恢复为新分支。

## 18. 开发检查

根目录提供：

    scripts\setup.ps1
    scripts\dev.ps1
    scripts\generate-api.ps1
    scripts\check.ps1
    scripts\build.ps1

check.ps1 至少执行：

后端：

- ruff format --check；
- ruff check；
- pyright；
- pytest。

前端：

- biome check；
- tsc --noEmit；
- vitest run；
- vite build。

关键交互变更再运行 Playwright。

## 19. 实施分期建议

### 阶段 1：工程和本地运行

- 项目骨架；
- FastAPI、React、Phaser；
- SQLite 与 Alembic；
- 启动器和 LocalAppData；
- 统一检查与打包。

### 阶段 2：AI Provider

- Provider/Model CRUD；
- OpenAI-compatible；
- DeepSeek 官方适配；
- 流式调用、JSON 和 Tool Calls；
- 调用日志和错误模型。

本阶段先建立可管理、可测试并能真实连接的全局 AI 基础，不依赖世界、角色或轮次表。世界级工作流任务选模仍在后续阶段接入，不把 Provider 配置复制到世界内。

### 阶段 3：世界、角色和地图

- 多世界和分支基础；
- 角色、资产绑定、属性和位置规则；
- 7 张 1920×1080 空白 JPG 与可编辑地图目录；
- 总地图键盘/鼠标导航和 6 张地点地图；
- 地图移动、碰撞、位置、返回总地图和时间；
- 自动保存基础。

### 阶段 4：轮次纵向切片

- 通用阶段/并行执行器；
- 第一版代码任务；
- 主角意图；
- 两类故事输出；
- 属性命令；
- 原子提交、取消和重试。

### 阶段 5：历史、快照与打包验收

- 时间线；
- 命名快照与分支；
- 中断恢复；
- Playwright；
- Windows onedir 打包和干净环境验证。

### 后续

- 工作流持久化和 CRUD；
- 最近 N 轮设置页面；
- 每角色短期记忆总结子 Agent。

## 20. 永久禁止引入

除非产品定义被用户明确修改，否则不得引入：

- 向量数据库、Embedding、语义检索和 RAG；
- Redis、Celery、外部消息队列；
- PostgreSQL；
- LangChain、LangGraph、CrewAI、AutoGen；
- 完整工作流引擎；
- Electron、Tauri、PySide6、pywebview；
- Redux、Zustand；
- 任意脚本执行；
- 微服务和云端部署；
- 登录、OAuth、JWT 和多用户权限。

## 21. 官方资料

- [Python 3.14.6](https://www.python.org/downloads/release/python-3146/)
- [PyInstaller Python 3.14 支持](https://pyinstaller.org/en/latest/CHANGES.html)
- [Node.js 发布计划](https://nodejs.org/en/about/previous-releases)
- [Node.js 24.18.0](https://nodejs.org/en/blog/release/v24.18.0)
- [Python sqlite3](https://docs.python.org/3/library/sqlite3.html)
- [FastAPI 版本策略](https://fastapi.tiangolo.com/deployment/versions/)
- [React 版本](https://react.dev/versions)
- [Phaser 4.2](https://phaser.io/news/2026/06/phaser-v4-2-0-released)
- [DeepSeek API 快速开始](https://api-docs.deepseek.com/)
- [DeepSeek Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion)
- [DeepSeek Tool Calls](https://api-docs.deepseek.com/guides/tool_calls)
