# AI 角色扮演生活模拟游戏：技术选型文档

> 文档状态：阶段 4 与阶段 5 合并设计确认后的技术基线
> 日期：2026-07-16
> 目标平台：Windows 11 x64
> 设计原则：使用能够完整交付产品的最小技术集合；必需能力不省略，无当前用途的实体不加入。

## 1. 决策摘要

采用模块化单体：

- 后端：Python、FastAPI、Pydantic、SQLAlchemy、Alembic、HTTPX；
- 数据库：SQLite；
- 前端：React、TypeScript、Vite、CSS Modules；
- 2D 游戏：Phaser 4；
- AI：DeepSeek 官方 API 与通用 OpenAI-compatible API；
- 打包：PyInstaller onedir；
- 运行：一个 Python 进程、一个 SQLite 数据库、一个系统浏览器页面。

项目内实现一个按地图并行的固定链路执行器。每张有角色地图独立串联“地点推演 → 属性与记忆分析”，所有地图链并行；第一版只存在这两个代码定义任务，不引入工作流引擎、消息队列、第三方多 Agent 框架或向量检索。

## 2. 运行时基线

| 项目 | 基线 |
|---|---|
| 操作系统 | Windows 11 x64 |
| Python | 3.14.6 |
| Python 项目工具 | uv |
| Node.js | 24.18.0 LTS |
| npm | 随 Node 24，初始化时锁定 |
| SQLite | 项目锁定的 Python 3.14.6 运行时自带版本 |
| 浏览器 | 当前受支持的 Edge、Chrome、Firefox |

选择依据：

- Python 3.14.6 是 2026-06-10 发布的 3.14 维护版；
- PyInstaller 从 6.15 起支持 Python 3.14；
- Node 24 当前处于 LTS，适合新项目；
- SQLite 跟随项目锁定的 Python 3.14.6 运行时自带版本，不单独替换或捆绑 SQLite DLL；启动、检查和打包记录实际 `sqlite3.sqlite_version`。

版本管理规则：

1. 根目录使用 .python-version 固定 Python 3.14.6；
2. uv.lock 与 package-lock.json 必须提交；
3. package.json 使用 engines 约束 Node 24，并固定 packageManager；
4. 构建脚本检查实际 Python、Node、npm 和 SQLite 版本；
5. 生产依赖必须由锁文件确定，不使用浮动安装；
6. 更新版本时同时更新本文、锁文件、测试和构建验证；
7. FastAPI 等快速迭代库固定到实际测试通过的版本，不把“永远最新”当作版本策略。

## 3. 总体架构

    系统默认浏览器
    └── React
        ├── 页面、表单、时间线和故事
        ├── 轮次进度、取消和重试
        └── Phaser 4 地图画布
                 │
                 │ REST + NDJSON
                 ▼
    FastAPI 模块化单体
    ├── provider
    ├── workflow
    ├── world
    ├── character
    ├── attribute
    ├── story
    ├── game
    └── db
        │              │
        ▼              ▼
    AI Provider      SQLite

生产环境只有：

- 一个 Python 进程；
- 一个应用级 HTTPX AsyncClient；
- 一个 SQLite 数据库；
- 一个由 FastAPI 提供的前端静态站点；
- 一个系统默认浏览器页面。

Node.js 只用于开发和构建，不进入生产运行时。

## 4. 后端技术

### 4.1 运行依赖

| 技术 | 用途 |
|---|---|
| FastAPI | REST、OpenAPI、静态资源、NDJSON |
| Uvicorn | 本地 ASGI 服务 |
| Pydantic 2 | API DTO、AI 输出、属性命令和配置校验 |
| SQLAlchemy 2.0 | SQLite ORM、连接和短事务 |
| Alembic 1.x | 数据库迁移 |
| HTTPX | 异步 AI API、连接池和流式响应 |

不使用 Tenacity。轮次任务需要显式处理取消、任务状态、输入快照和输出是否已产生，项目内小型重试函数更清晰。

### 4.2 开发依赖

- pytest；
- pytest-asyncio；
- respx；
- Ruff；
- Pyright；
- PyInstaller。

### 4.3 数据库访问方式

使用 SQLAlchemy 同步 Session 和标准 SQLite 驱动。

理由：

- 数据库操作均为本地短事务；
- 等待 AI 时不持有连接或事务；
- 单用户、单进程场景不需要 aiosqlite 带来的额外抽象；
- AI 并发由 asyncio 管理，与数据库并发无关。

API 路由只接收输入、调用 Service 并转换 HTTP 响应。业务逻辑不得进入路由。

## 5. 前端与游戏技术

### 5.1 前端

| 技术 | 用途 |
|---|---|
| React 19.2 | 页面和交互 UI |
| TypeScript | 类型安全 |
| React Router | 页面路由 |
| Vite | 开发与生产构建 |
| 原生 Fetch | REST 与 NDJSON |
| CSS Modules | 样式隔离 |
| openapi-typescript | 从 OpenAPI 生成前端 API 类型 |
| Biome | 格式化和静态检查 |

状态优先级：

1. 组件本地 state；
2. useReducer；
3. 少量 Context。

不加入 Redux、Zustand、Axios、Tailwind、ESLint、Prettier或大型 UI 组件库。

### 5.2 Phaser 4

Phaser 负责：

- Tilemap 和场景；
- 总地图主角标记、地点锚点和基础移动；
- 键盘或点击移动；
- Camera；
- 边界与障碍碰撞；
- 位置点和场景切换；
- 当前地点的角色可见性。

Phaser 不负责：

- 时间推进；
- AI 请求；
- 角色属性和记忆；
- 数据库访问；
- 世界事实裁决；
- Provider 与模型配置。

React 与 Phaser 通过项目内类型化 GameBridge 通信，不引入事件总线库。

地点角色立绘使用 React 图片层渲染 1:1 相框，便于统一处理 GIF、object-fit: contain、错误状态和无障碍名称；Phaser 不复制角色可见性判断。未来场景 CG 也由 React 使用独占全屏图片层展示。

### 5.3 第一版地图实现

场景固定为：

| scene_id  | 显示名称 | 类型 | 背景文件 |
|-----------|---|---|---|
| the_world_map | 总地图 | 导航总图 | 总地图 |
| the_home      | 家 | 地点 | 家 |
| the_dungeon      | 地下城 | 地点 | 地下城 |
| the_mall      | 商店街 | 地点 | 商店街 |
| the_guild       | 冒险者工会 | 地点 | 冒险者工会 |
| the_hotel | 旅店 | 地点 | 旅店 |
| the_school    | 学校 | 地点 | 学校 |

资源规则：

- 7 张默认地图来自仓库 `resource`，像素尺寸可变但必须为横向 16:9；
- map_manifest.json 保存中文文件主体而不是扩展名，先查找 `.jpg`，再查找 `.png`，同名冲突时优先 JPG；
- 首次启动时，把缺失的默认地图复制到 LocalAppData 的可编辑内容目录；
- 已存在的同名地图图片绝不被升级或启动流程覆盖；
- FastAPI 从可编辑内容目录提供地图图片；
- map_manifest.json 使用稳定 ASCII scene_id 关联中文文件主体、地点顺序和归一化总地图锚点；
- 图片不进入 SQLite；
- 地图静态响应使用 Cache-Control: no-cache，使同名文件替换后在刷新页面时重新验证；
- 文件缺失、损坏或比例不正确时记录可理解错误，并回退到内置空白占位图。

渲染结构：

1. Phaser Canvas 填满游戏视口；
2. 当前地图 JPG 或 PNG 作为最底层背景；
3. 总地图主角标记、地点锚点和必要交互提示由 Phaser 绘制；
4. 地点角色 1:1 立绘相框、时间、行动输入、故事、进度及“地图”按钮由 React 以绝对定位覆盖在 Canvas 上；
5. 16:9 视口完整贴合，其他比例使用 cover 居中裁切；
6. 背景、锚点、碰撞配置和角色位置共享同一逻辑坐标到视口的变换，不能依赖图片绝对像素；
7. 所有关键锚点和浮动 UI 位于 16:9 安全区域。

总地图输入状态机：

- Left/Right 将目标切换到相邻地点，并播放主角小型立绘的移动动画；
- 第一次点击地点会选择该地点并移动主角；
- 主角到达已选地点后，再次点击或按 Enter 执行 enter_location；
- enter_location 切换到对应地点场景；
- 地点场景左上角的 React“地图”按钮执行 return_to_world_map；
- move、select_location、enter_location、return_to_world_map 都不推进故事时间。

地点地图只在玩家提交文字意图时创建 turn_run。进入地点后，后端根据该地点的 location_id 返回主角优先、NPC 按稳定角色 ID 排序的最多 6 个可见角色，前端不得自行推断角色是否同处一地。溢出 NPC 使用受控离屏位置事实，仍参与推演但不能直接互动。

### 5.4 角色图片与场景 CG

角色资源目录为 `%LOCALAPPDATA%\ClearSkyEngine\content\characters\{角色名称}\`。默认立绘主体必须等于角色名称；其他同目录图片主体是未来差分或 CG 描述。解析器按 `.jpg`、`.jpeg`、`.png`、`.gif`、`.webp`、`.avif`、`.bmp` 的稳定顺序选择第一张同名资源，禁止 SVG。

阶段 3 只渲染默认立绘：单行、主角第一、NPC 按稳定角色 ID、每地点最多 6 个。每张图片使用 1:1 相框和 contain，不裁切、不拉伸，窗口过窄时提示调整尺寸。未来资源选择器返回角色 ID、资源主体和 `portrait` 或 `scene_cg` 展示模式；CG 使用深色幕布上的全屏 contain，并隐藏其他图片层。本阶段不实现 AI 选图。

## 6. 后端模块边界

建议按业务能力组织：

    backend/src/app/
    ├── main.py
    ├── launcher.py
    ├── config.py
    ├── api/
    ├── ai/
    │   ├── models.py
    │   ├── errors.py
    │   ├── provider.py
    │   ├── registry.py
    │   └── deepseek.py
    ├── workflow/
    │   ├── models.py
    │   ├── definitions.py
    │   ├── runner.py
    │   └── context.py
    ├── world/
    ├── character/
    ├── attribute/
    ├── story/
    ├── game/
    ├── resources/
    ├── db/
    └── static/

规则：

- 不创建 domain/application/infrastructure/adapter 四层目录；
- Service 只有一个实现时不抽取接口；
- Store 直接使用 SQLAlchemy Session；
- 只有 Provider 和 Skill 确实存在多个实现，因此为它们定义稳定协议；
- 依赖通过普通构造函数传入，不使用依赖注入框架。
- `character/` 拥有全局角色主表、世界引用和删除保护；`attribute/` 拥有最终值合成、更新意图 Schema 和通用更新函数；`game/` 拥有地点容量和位置事实；`resources/` 只负责图片发现、复制、校验和安全响应。

## 7. AI Provider 设计

### 7.1 Provider 层

第一版包含：

- OpenAICompatibleProvider；
- DeepSeekProvider，复用兼容协议，只覆盖 DeepSeek 专有能力；
- ProviderRegistry。

ProviderRegistry 只完成 provider_type 到无业务状态 Provider 实例的映射。

所有 Provider 共享应用级 HTTPX AsyncClient。禁止每次请求创建新客户端。

### 7.2 DeepSeek

首发模型：

- deepseek-v4-pro；
- deepseek-v4-flash。

支持：

- POST /chat/completions；
- 普通和流式输出；
- reasoning_content；
- thinking 模式；
- JSON Output；
- Tool Calls；
- token usage；
- DeepSeek finish_reason；
- SSE keep-alive 注释和空行；
- 官方错误响应映射。

旧模型别名 deepseek-chat 和 deepseek-reasoner 不作为产品默认值。

### 7.3 统一请求模型

统一请求至少包括：

- model_id；
- messages；
- temperature；
- max_output_tokens；
- thinking 配置；
- tools；
- response_format；
- provider_options。

统一响应至少包括：

- 文本；
- reasoning 内容；
- tool calls；
- finish reason；
- usage；
- provider_id；
- model_id。

provider_options 是任务调用侧的任意 Provider 参数 JSON 逃生口，不能把每个厂商字段塞进公共 DTO。其根节点必须是对象，内容按顶层字段原样合并到请求；不得与公共 DTO 字段或程序拥有的 model、messages/input、认证、stream、tools、tool_choice 和结构化输出 Schema 等字段重名。前后端只校验 JSON 结构和冲突，不尝试穷举未知厂商参数的语义。

### 7.4 模型选择与失败

- 两个应用级 AI 任务都必须人工选择一个模型；
- 不提供自动模型选择；
- 不提供模型 fallback；
- 每个失败节点首次调用后最多再重试 2 次，总计最多 3 次；只重试失败节点；
- 超时、临时网络错误、限流、Provider 服务端错误和结构化输出无效可以重试；
- 用户取消、缺少模型、配置错误、认证或权限错误不重试；
- 任一节点耗尽重试后整轮失败，不自动更换模型。

## 8. 按地图并行链路

### 8.1 第一版能力

执行器只支持两个稳定任务定义：

- `location_simulation`：每张地图调用一次，输出互动分组、客观事件和地图内全部角色独立纪事；
- `attribute_memory_analysis`：在同一地图地点推演成功后调用一次，输出地图内全部角色的属性更新意图与本轮记忆新增摘要。

任务定义放在 workflow/definitions.py。所有有角色地图通过 asyncio.TaskGroup 并行，每张地图内部严格串联两个节点；主角意图只注入主角所在地图。共享 asyncio.Semaphore 控制总调用并发，取消使用 cooperative cancellation。第一版全应用最多一个活动轮次。

两个任务的模型与调用参数持久化为应用级开发商配置，不属于世界或分支。每个任务独立保存模型、温度、最大输出 Tokens、思考强度、超时、额外提示词、结构化输出策略、自由 Provider JSON 和版本；属性与记忆任务另存每角色摘要目标字数与硬上限，初始值为 20 和 50。保存后对下一次新轮次生效，运行中任务使用内存冻结副本。

任务需要结构化结果时，模型能力声明支持原生 JSON/结构化输出就启用，否则使用提示词约束；两条路径最终经过同一 Pydantic Schema 校验。缺少模型选择、参数非法或自由 JSON 冲突时拒绝启动轮次。

### 8.2 不实现

- 任意 DAG 或工作流 CRUD；
- 用户循环；
- 条件脚本；
- Python、JavaScript 或 Shell 节点；
- Celery、Redis 和外部队列；
- 第三方 Agent 编排框架。

### 8.3 未来演进

未来若需要可视化编辑工作流，必须另行设计；当前全局页面只编辑两个固定任务的调用配置，不得演变为任意节点、DAG、循环或脚本执行器。

## 9. 一轮执行与一致性

### 9.1 创建内存运行

POST 创建进程内运行并立即返回临时 run_id。系统在短只读会话中读取并冻结：world_id、隐藏内部 branch_id、世界版本、当前时间、玩家意图、程序计算后的全部角色位置、角色配置与最终属性、每角色长期记忆、最近 5 个有效轮次可知原文，以及两个任务的应用级配置版本和完整内存副本。冻结完成后关闭数据库会话。

运行、节点状态、中间输出和错误只存在于当前进程内存，不写 turn_run 或 turn_task_run 表。run_id 只在当前进程和当前页面连接期间有效。

### 9.2 执行

- 后台 asyncio Task 执行流水线；
- 每张地图只读取该地图上下文，主角意图只进入主角地图；
- 地点推演成功后，该地图立即进入属性与记忆分析，不等待其他地图；
- 浏览器通过 NDJSON 获得地图、节点、尝试次数、完成数量和耗时；
- 当前页面连接断开、刷新或应用退出时取消运行并清除全部临时结果。

### 9.3 最终提交

在一个短事务中：

1. 检查 world 与隐藏内部 branch 仍处于输入快照版本；
2. 验证故事结构；
3. 验证地图分组、角色覆盖和信息边界；
4. 验证全部属性意图和每角色记忆摘要；
5. 调用通用属性函数更新世界角色变化表；
6. 按 `(world_id, role_id)` 更新记忆表，首次写入摘要，后续追加 `__` 与摘要；
7. 写入不可变 turn、角色纪事、事件和状态变化；
8. 保存 state_after_json；
9. 更新隐藏内部分支当前状态与版本；
10. 推进时间并计算下一时间段 NPC 位置。

任何一步失败都回滚整个最终事务。

### 9.4 取消、重试与丢弃

- 自动重试沿用同一内存输入与配置副本；
- 只重试失败节点，当前运行内成功节点的内存输出可以复用；
- 取消、失败、连接中断、刷新或应用退出都不产生 turn；
- 运行结束后立即释放所有临时结果；
- 应用重启无需恢复或修正运行记录，只从最近成功 turn 继续；
- 当前页面仍打开时保留玩家原始意图，允许重新提交一个新运行。

## 10. 时间、位置与信息边界

### 10.1 时间

使用枚举：

- morning；
- midday；
- evening；
- night。

advance_time 是纯函数，night 后 day 加一并回到 morning。

世界固定从星期一、Day 1 morning 开始。当前星期只由 day 相对星期一推导，不维护可写星期字段。位置规则使用推导结果判断 weekday_mask。

### 10.2 位置

地图和位置目录由开发者 JSON 定义，使用稳定 location_id。

NPC 位置规则由数据库保存，程序在轮次开始前计算角色位置。AI 只能读取位置事实，不能任意更改地图或让角色瞬移。每张地图永久为主角预留一位，最多分配 5 名 NPC。

首次按规则分配时，超出目标地图容量的 NPC 进入二次缓存。全部地图完成首次分配后，用由冻结世界、日期和时间段派生的确定性种子打乱缓存，并按稳定地图顺序逐个填补未满地图；全部地图满员后的剩余角色写入显式 offline 位置状态。算法必须是无 I/O 纯函数，并覆盖空缓存、全部满员、没有可用地图和单角色等边界。成功轮次推进时间后才计算下一时段 NPC 位置；主角移动不触发 NPC 重分配。

### 10.3 信息隔离

每个 AI 任务使用 ContextBuilder。地点推演 ContextBuilder 只选择当前地图角色、每角色人设与最终属性、该角色长期记忆、最近 5 个有效轮次可知原文、明确被告知事实、世界公开事实以及当前时间地点；主角意图只在主角地图出现。属性与记忆分析只选择该地图本轮纪事与事件、每角色最终属性和对应更新规则。

有效轮次按角色定义：角色必须被分配到地图并获得 AI 纪事；offline 固定说明不计入，独处角色的极短 AI 纪事计入。

地点推演虽然一次生成地图内全部角色纪事，但每段输出必须绑定 role_id，并通过可知事件 ID 或感知结构描述信息来源。后续上下文从程序过滤的结构化事实和该角色自己的记忆构建，绝不读取其他角色纪事。玩家能够查看全部独立纪事不改变角色知识。

## 11. 属性与 Skill

### 11.1 基础值、世界变化与最终值

角色主表使用单层 JSON 保存基础值，世界角色变化表使用单层 JSON 保存变化值。类型定义支持 integer、number、string、boolean 和 enum，不得把所有值当成字符串处理。

最终值合成是 `attribute/` 中的无 I/O 纯函数：只遍历基础 key；数值执行基础值加变化量；其他类型使用世界覆盖值；缺少世界值时使用基础值；遗留 key 或类型不匹配值忽略。AI、API 和世界角色页面只能读取最终值。

### 11.2 属性定义与状态命令

每个基础属性定义稳定 key、显示名称、类型、基础值、含义、允许操作、更新规则、可选约束和可选正反例。key 创建后不可修改。

通用命令为 replace、increment、decrement，包含：

- role_id；
- attribute_key；
- operation；
- value 或 amount；
- reason；
- source_event_id，可为空；
- expected_version。

程序检查角色引用、基础属性存在、操作允许、类型、上下限和版本后执行，不替 AI 重做“本轮内容是否符合更新规则”的语义判断，也不强制属性变化必须来自客观事件。数值 replace 根据目标最终值反算变化量；非数值只允许 replace。

### 11.3 Skill

属性与记忆分析任务按地图接收本地图全部角色的本轮内容、当前最终值和最小规则上下文，在同一结构化响应中为每名角色返回 `attribute_update_intents` 和 `memory_append`。离线角色不运行该任务。只读 Skill 支持读取属性定义；写入 Skill 只收集结构化结果，不直接修改数据库。

`memory_append` 默认目标 20 字、硬上限 50 字，两者读取应用级任务设置；内容不得包含保留分隔符 `__`。最终短事务根据同一响应的不同字段分别调用唯一通用属性更新函数和记忆追加函数。任一意图或摘要无效时该地图节点失败，不得部分更新或推进时间。不得为动态属性生成专用代码，也不能绕过统一命令层执行任意 SQL、Shell、文件写入或动态代码。

第一版不向 AI 提供：

- 任意 Shell；
- 任意文件写入或删除；
- 任意 SQL；
- 动态下载和执行代码。

## 12. 数据库设计

### 12.1 全局表

#### ai_provider

- id；
- name；
- provider_type；
- base_url；
- api_key；
- enabled；
- extra_json；
- created_at；
- updated_at。

API Key 明文保存，但查询 API 只返回 has_api_key。

#### ai_model

- id；
- provider_id；
- display_name；
- remote_model；
- capabilities_json；
- enabled；
- created_at；
- updated_at。

模型表只保存模型身份和能力声明，不保存温度、输出 Tokens、思考强度等业务调用参数。现有 defaults_json 通过新 Alembic 迁移移除，迁移前后都不得作为任务参数来源。

#### ai_task_setting

- id；
- task_key，全局唯一，只允许 location_simulation 或 attribute_memory_analysis；
- model_id；
- temperature，可为空；
- max_output_tokens，可为空；
- reasoning_effort，可为空；
- timeout_seconds；
- extra_prompt；
- structured_output_mode；
- provider_options_json，根节点对象；
- memory_target_chars，仅属性与记忆任务使用，初始为 20；
- memory_max_chars，仅属性与记忆任务使用，初始为 50；
- version；
- updated_at。

任务设置属于应用全局，不携带 world_id 或 branch_id。保存时校验公共字段、自由 JSON 根类型、保留字段冲突以及摘要目标不大于硬上限；保存成功后递增 version。

#### app_setting

- key；
- value_json；
- updated_at。

#### asset_catalog

可由静态清单加载，不强制建立数据库表。若需要在 UI 中筛选，保存稳定 asset_id、类型和资源路径的只读索引；资产文件本身不进入 SQLite。

第一版地图目录由 map_manifest.json 管理，不建立可编辑地图数据库表。

#### role

- id；
- name，全局唯一且创建后不可修改；
- persona；
- system_prompt；
- world_book；
- base_values_json，单层 key-value；
- attribute_types_json；
- attribute_labels_json；
- attribute_descriptions_json；
- attribute_update_rules_json；
- attribute_constraints_json；
- attribute_allowed_operations_json；
- attribute_examples_json；
- version；
- created_at；
- updated_at。

`base_values_json` 的 key 集合是属性字段事实来源。类型、显示名、含义、更新规则和允许操作 JSON 必须具有完全相同的 key；约束和示例只能引用这些 key。Service 在一个事务中校验并保存整组定义。提供给世界与 AI 的最终角色属性必须解析成单层 key-value。角色被任何世界引用时禁止删除。

### 12.2 世界与分支

#### world

- id；
- active_branch_id；
- created_at；
- updated_at；
- last_played_at。

世界显示名称由 id 派生为“世界 {id}”，不单独保存可编辑名称。初始星期固定为星期一，初始时间固定为 Day 1 morning。

#### world_branch

- id；
- world_id；
- name；
- parent_branch_id；
- fork_turn_id；
- head_turn_id；
- day；
- time_slot；
- current_location_id，活动分支中的主角当前地点；
- state_version；
- created_at；
- updated_at。

当前版本只创建和使用内部初始分支，不开放分支创建、切换、快照或存档点 API/UI，也不建立 world_snapshot 表。未来开放分支前必须另行设计迁移，把角色变化值、长期记忆和其他分支状态全部扩展为分支隔离。

### 12.3 世界当前角色状态

#### world_role_state

- world_id；
- role_id；
- kind：player 或 npc；
- enabled；
- change_values_json，单层 key-value；
- version；
- created_at；
- updated_at。

约束：

- `(world_id, role_id)` 唯一，每个世界每个角色只有一条变化记录；
- 每个 world 只能有一个 kind=player；
- player 不允许删除；
- 删除 world 级联删除状态，删除被引用 role 被拒绝。

#### world_role_memory

- world_id；
- role_id；
- memory，SQLite TEXT，默认空字符串；
- version；
- created_at；
- updated_at。

`(world_id, role_id)` 唯一并外键引用 world_role_state。首次摘要直接写入 memory；后续在同一事务中写为 `memory || '__' || memory_append`。摘要不得包含 `__`，删除世界或世界角色时级联删除记忆。

#### character_location_rule

- id；
- world_id；
- role_id；
- weekday_mask；
- time_slot；
- mode：fixed 或 random；
- priority；
- enabled。

#### character_location_candidate

- rule_id；
- location_id；
- weight。

位置计算为每个可进入地点保留 1 个主角位置并最多分配 5 名 NPC。超出首次目标地图容量的 NPC 进入二次缓存，确定性打乱后按稳定地图顺序补位；最终剩余角色使用非空的显式 offline 状态。

### 12.4 轮次运行

未完成运行只使用进程内 `TurnRun` 与 `MapChainRun` Pydantic/普通类型，不建立 turn_run 或 turn_task_run 表。类型至少包含临时 run_id、冻结输入、配置副本、取消信号、地图节点状态、尝试次数、内存输出和安全错误；生命周期结束即释放。

### 12.5 成功轮次

#### turn

- id；
- world_id；
- branch_id；
- parent_turn_id；
- day；
- time_slot；
- player_intent；
- objective_events_json；
- state_after_json；
- ai_execution_meta_json，只包含任务配置版本、模型 ID 和安全哈希；
- created_at。

#### turn_story

- id；
- turn_id；
- role_id，非空；
- content；
- sort_order。

#### turn_event

- id；
- turn_id；
- location_id；
- event_type；
- fact_json；
- created_at。

#### turn_event_participant

- event_id；
- role_id；
- knowledge_level；
- perspective_notes。

#### state_change

- id；
- turn_id；
- event_id，可为空；
- role_id；
- attribute_key；
- operation；
- old_value_json；
- operand_json；
- new_value_json；
- reason。

每轮故事、事件、状态变化、世界角色属性、world_role_memory 追加和 state_after_json 在同一数据库事务中完成一次逻辑保存。自由 Provider JSON、API Key、完整 Prompt 和敏感响应不得进入 ai_execution_meta_json。

## 13. SQLite 配置

每个连接启用：

    PRAGMA foreign_keys = ON;
    PRAGMA journal_mode = WAL;
    PRAGMA synchronous = FULL;
    PRAGMA busy_timeout = 5000;

规则：

- 等待 AI 时不得保持事务；
- 所有 schema 变更通过 Alembic；
- 启动时执行迁移前先备份数据库；
- 数据库文件只写入 LocalAppData；
- 图片、音频、地图和精灵不得以 BLOB 保存；
- 构建与启动记录并检查 `sqlite3.sqlite_version`；
- 生产包必须使用 Python 3.14.6 自带 SQLite，不单独替换运行时 DLL。

## 14. API 设计

至少包括：

### 全局配置

- /api/providers；
- /api/models；
- /api/ai-task-settings；
- /api/app-settings；
- /api/health；
- /api/app/shutdown。

### 世界

- /api/worlds；
- /api/worlds/{world_id}/turns。

当前版本不公开分支、快照、存档点或独立时间线 API。

`POST /api/worlds` 只接受已有全局角色的 `protagonist_role_id`。世界创建不得按名称重复插入或修改角色主记录；同一全局角色可以被多个隔离世界引用。

### 角色与属性

- /api/roles；
- /api/roles/{role_id}/references；
- /api/worlds/{world_id}/roles；
- /api/worlds/{world_id}/roles/{role_id}/location-rules；
- /api/worlds/{world_id}/roles/{role_id}/effective-attributes；
- /api/assets/roles。

角色主表写 API 只接受经过 Pydantic 校验的属性定义。世界角色 API 不提供直接修改 change_values_json 的入口；阶段 4 的变化值只能由最终结算服务内部写入。

### 轮次

- POST /api/worlds/{world_id}/turn-runs；
- GET /api/turn-runs/{run_id}；
- GET /api/turn-runs/{run_id}/stream；
- POST /api/turn-runs/{run_id}/cancel。

run_id 是进程内临时标识，不对应数据库记录。stream 使用 application/x-ndjson，每行一个完整 JSON 事件；连接断开触发取消与临时结果清理。自动重试由执行器内部完成，失败后用户通过保留的意图重新创建运行，不提供持久化 retry API。

## 15. 本地安全边界

- Uvicorn 只绑定 127.0.0.1；
- 不启用公网或局域网访问；
- 生产环境不启用宽松 CORS；
- 校验 Host 只能是当前 loopback 地址和端口；
- 修改型 API 校验同源 Origin；
- shutdown 等敏感操作使用启动期随机令牌；
- API Key 不返回前端、不进入日志、不进入异常；
- API Key 可以替换或清空；
- 数据库备份包含明文 API Key，UI 必须提示风险。

这些措施不是用户登录系统，不引入 JWT、OAuth 或权限模型。

## 16. 打包与目录

第一版使用 PyInstaller onedir。

启动流程：

1. 确定 LocalAppData 数据目录；
2. 初始化日志；
3. 检查并备份数据库；
4. 执行 Alembic upgrade head；
5. 选择空闲端口；
6. 启动 Uvicorn；
7. 打开默认浏览器。

可写目录：

    %LOCALAPPDATA%\ClearSkyEngine\
    ├── data\app.db
    ├── content\
    │   ├── maps\
    │   │   ├── 总地图.{jpg|png}
    │   │   ├── 家.{jpg|png}
    │   │   ├── 地下城.{jpg|png}
    │   │   ├── 商店街.{jpg|png}
    │   │   ├── 冒险者工会.{jpg|png}
    │   │   ├── 旅店.{jpg|png}
    │   │   └── 学校.{jpg|png}
    │   └── characters\
    │       └── {角色名称}\
    │           ├── {角色名称}.{jpg|jpeg|png|gif|webp|avif|bmp}
    │           └── {描述}.{jpg|jpeg|png|gif|webp|avif|bmp}
    ├── logs\
    └── backups\

前端构建产物复制到 backend/src/app/static，生产环境不启动 Node.js。

首次启动只复制缺失的默认地图；用户或开发者替换的同名图片必须保留。替换后刷新浏览器或重启应用即可生效，不提供运行中热更新。角色目录由资源扫描器读取，不创建数据库种子角色。

## 17. 测试策略

### 17.1 后端

必须覆盖：

- OpenAI-compatible 请求、响应和流事件；
- DeepSeek thinking、JSON、Tool Calls、keep-alive 和错误映射；
- 地图链内部地点推演到属性与记忆分析的顺序；
- 多地图链并行；
- 并发上限；
- 取消、连接断开、错误分类和每节点最多三次尝试；
- 运行期冻结输入与成功节点内存复用；
- 时间推进；
- NPC 位置规则；
- 每地点预留主角位、最多 5 名 NPC、二次缓存确定性补位和显式 offline 状态；
- 角色信息隔离；
- 角色主表与世界变化值合成，包括新增、删除、类型变化和遗留 key；
- 地图级属性分析、属性类型、允许操作、上下限和版本校验；
- world_role_memory 唯一约束、20/50 配置、`__` 追加和属性与记忆原子更新；
- 角色名称、唯一主角、`(world_id, role_id)` 和删除保护约束；
- 世界创建只引用已有角色且不重复插入角色主记录；
- 地图与角色资源优先级、损坏回退、变分辨率适配和首次复制不覆盖；
- 轮次原子提交；
- 世界隔离；
- Alembic 从空库升级；
- API Key 不出现在查询与日志中。

离线回归使用 respx。真实付费 API 不进入自动测试，但 AI 关键链路完成后必须另行运行真实测试，mock 不能替代。

### 17.2 前端

Vitest 与 React Testing Library 覆盖：

- API 转换；
- NDJSON 解析；
- 轮次进度；
- 取消和重试状态；
- 角色库属性表单、引用保护和世界角色只读最终值；
- 轮次卡片旧轮次折叠和最新轮次展开；
- 全局固定 AI 任务配置表单与自由 Provider JSON 编辑器；
- GameBridge；
- 地图清单解析；
- 总地图 Left/Right、鼠标选择、Enter 进入状态机；
- 地点地图返回总地图；
- 位置规则纯函数。

### 17.3 端到端

合并阶段不建设使用模拟 AI 的默认离线 Playwright。轮次端到端测试直接使用用户提供的真实 Provider 与模型，会产生费用，因此不进入 scripts/check.ps1；但它是每个可玩纵向切片和最终验收的高优先级人工触发门禁，无凭据时只能报告“真实 AI 浏览器验收未验证”。

真实 AI Playwright 必须从网页完成：

1. 配置 Provider、模型、地点推演和属性与记忆分析两个全局任务；
2. 创建全局角色与属性规则，再以已有角色 ID 创建世界，断言角色总数、ID 和内容不变，永久回归世界创建重复插入角色唯一键冲突；
3. 加入 NPC、配置位置规则并验证真实立绘；
4. 移动主角且时间不变；
5. 提交至少两轮自由意图并观察每地图节点进度；
6. 验证同地图互动、跨地图不互动、NPC 自由互动和全部角色独立纪事；
7. 验证客观事件、属性变化、world_role_memory 的 `__` 追加、时间推进和下一时段位置；
8. 验证旧轮次卡片折叠、最新轮次展开。

`resource` 中的 `天.jpg`、`莫莉莉.png`、`安可儿.png` 只复制到隔离测试内容目录，不创建产品种子数据。测试不得记录密钥、完整 Prompt 或敏感响应。

异常闭环不要求通过人为制造真实 Provider 故障完成 Playwright。实现完成后必须对错误分类、最多三次尝试、取消、连接断开、内存临时状态和原子提交执行一次专项 code review；真实 Playwright 自然遇到的异常必须保留为回归案例，修复后用真实流程复验。

## 18. 开发检查

根目录提供：

    scripts\setup.ps1
    scripts\dev.ps1
    scripts\generate-api.ps1
    scripts\check.ps1
    scripts\build.ps1

check.ps1 至少执行：

后端：

- ruff format --check；
- ruff check；
- pyright；
- pytest。

前端：

- biome check；
- tsc --noEmit；
- vitest run；
- vite build。

关键交互变更再运行 Playwright。

## 19. 实施分期建议

### 阶段 1：工程和本地运行

- 项目骨架；
- FastAPI、React、Phaser；
- SQLite 与 Alembic；
- 启动器和 LocalAppData；
- 统一检查与打包。

### 阶段 2：AI Provider

- Provider/Model CRUD；
- OpenAI-compatible；
- DeepSeek 官方适配；
- 流式调用、JSON 和 Tool Calls；
- 调用日志和错误模型。

本阶段先建立可管理、可测试并能真实连接的全局 AI 基础，不依赖世界、角色或轮次表。后续合并阶段增加应用级固定 AI 任务配置，不把 Provider 或调用参数复制到世界内。

### 阶段 3：世界、角色和地图

- 自动命名多世界和内部初始分支基础；
- 全局角色主表、世界角色变化表、最终值合成和删除保护；
- 动态属性定义、更新意图 Schema 和通用更新纯函数，不接入故事 AI；
- 来自 `resource` 的 7 张可变像素 16:9 地图与 JPG/PNG 可编辑目录；
- 角色目录扫描、常用安全位图格式、1:1 contain 立绘和 6 人地点容量；
- 总地图键盘/鼠标导航和 6 张地点地图；
- 地图移动、基础碰撞、位置、返回总地图和时间；
- 通过网页使用三张真实测试立绘完成 Playwright，不创建产品种子数据；
- 自动保存基础。

### 合并阶段 4+5：完整可玩纵向闭环与打包验收

- 两个应用级固定 AI 任务及统一配置页面；
- 每地图“地点推演 → 属性与记忆分析”链路和多地图并行；
- 主角自由意图、互动分组、全部角色独立纪事和客观事件；
- 二次缓存位置分配、显式 offline 状态和主角永久预留位；
- 地图级属性命令、world_role_memory 长文本 `__` 追加和受控 Skill；
- 应用级任务参数、自由 Provider JSON、原生结构化输出能力与提示词 fallback；
- 每节点最多三次尝试、取消、连接中断和未完成运行内存丢弃；
- 成功轮次原子提交、单线自动保存和游戏主界面可折叠历轮故事；
- 只使用真实 AI 的 Playwright 完整可玩闭环；
- 异常路径专项 code review；
- Windows onedir 打包和干净环境验证。

### 后续

- 任意工作流、DAG 或可视化编排；
- 最近 N 轮设置页面；
- 长期记忆整合、去重、压缩和重点强化；
- 命名快照、存档点和分支时间线。

## 20. 永久禁止引入

除非产品定义被用户明确修改，否则不得引入：

- 向量数据库、Embedding、语义检索和 RAG；
- Redis、Celery、外部消息队列；
- PostgreSQL；
- LangChain、LangGraph、CrewAI、AutoGen；
- 完整工作流引擎；
- Electron、Tauri、PySide6、pywebview；
- Redux、Zustand；
- 任意脚本执行；
- 微服务和云端部署；
- 登录、OAuth、JWT 和多用户权限。

## 21. 官方资料

- [Python 3.14.6](https://www.python.org/downloads/release/python-3146/)
- [PyInstaller Python 3.14 支持](https://pyinstaller.org/en/latest/CHANGES.html)
- [Node.js 发布计划](https://nodejs.org/en/about/previous-releases)
- [Node.js 24.18.0](https://nodejs.org/en/blog/release/v24.18.0)
- [Python sqlite3](https://docs.python.org/3/library/sqlite3.html)
- [FastAPI 版本策略](https://fastapi.tiangolo.com/deployment/versions/)
- [React 版本](https://react.dev/versions)
- [Phaser 4.2](https://phaser.io/news/2026/06/phaser-v4-2-0-released)
- [DeepSeek API 快速开始](https://api-docs.deepseek.com/)
- [DeepSeek Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion)
- [DeepSeek Tool Calls](https://api-docs.deepseek.com/guides/tool_calls)
