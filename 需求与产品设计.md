# AI 角色扮演生活模拟游戏：需求与产品设计

> 文档状态：阶段 4 与阶段 5 合并设计经逐项核对后的产品基线
> 日期：2026-07-16
> 适用范围：第一版产品及已明确的后续演进
> 本文回答“做什么、为谁做、如何使用、什么算完成”；技术实现依据见《技术选型文档》。

## 1. 产品定义

本产品是一款运行于 Windows 11 的单机 AI 角色扮演与生活模拟游戏。

游戏使用开发者制作的 2D 地图、美术资产和交互内容，为 AI 角色提供统一、可验证的时间与空间。角色只有处于同一地点时才能直接相遇和互动，从而解决传统纯文字角色扮演中常见的空间位置不分、时间概念薄弱、角色无故获得场外信息等问题。

产品不是通用聊天工具，也不是地图、剧情或美术创作工具。用户配置的是 AI 角色扮演所需的文字内容、属性、位置规则和模型；地图、剧情框架、角色视觉资产和全部 2D 游戏内容由开发者制作并随游戏交付。

## 2. 产品目标

第一版必须实现以下结果：

1. 玩家可以在开发者制作的 2D 地图中控制主角移动。
2. 玩家到达某个地点后，可以输入主角本轮意图，触发一次完整世界推演。
3. 主角和 NPC 根据各自人设、属性、关系、位置和记忆自主行动，而不是服从主角的命令。
4. 所有角色都受到同一时间、空间和客观世界事实约束。
5. 每轮生成全部启用角色各自独立的角色纪事，并可靠更新角色属性、长期记忆和世界状态。
6. 用户可以配置角色、属性、NPC 位置规则、Provider、模型和应用级 AI 任务调用参数。
7. 支持多个相互隔离的单线世界、成功轮次自动保存和主界面历轮故事回看；分支、快照和存档点留待后续设计。
8. 在 Windows 11 上以一个本地后台进程运行，并自动打开系统默认浏览器。

## 3. 目标用户与使用前提

目标用户是熟悉 AI 角色扮演、愿意配置人设和提示词，并希望获得具有真实时间与空间约束的动态生活模拟体验的单机玩家。

使用前提：

- 用户自行准备 DeepSeek 或 OpenAI-compatible API；
- 用户理解 AI 生成内容存在随机性；
- 用户接受单轮世界推演可能耗时 1–3 分钟；
- 用户接受 API Key 明文保存在本地 SQLite 的产品风险；
- 应用不提供云端账号、多人协作或联网存档。

## 4. 核心产品原则

### 4.1 空间是真实约束

- 地图位置是世界事实，不是故事中的自由文本。
- 只有物理意义上位于同一地点的角色才能直接见面、交谈或共同经历事件。
- AI 不得通过叙事让角色无条件跨越位置限制。

### 4.2 时间是离散回合

世界时间固定为四个时间段：

    晨间 → 午间 → 傍晚 → 夜晚 → 次日晨间

- 晨间对应约 08:00–11:00；
- 午间对应约 11:00–17:30，不再拆分上午、中午和下午；
- 傍晚对应约 17:30 至夜幕降临前；
- 夜晚对应约 19:30–23:59；
- 夜晚结束后天数加一，并进入次日晨间。

地图移动、查看页面和编辑设置不推进时间。只有玩家提交一次主角行为意图并成功生成、结算故事，才算一次行动并推进一个时间段。

### 4.3 角色拥有自主性

- 主角意图只代表玩家希望主角尝试做什么；
- NPC 根据自己的人设、属性、关系和记忆决定回应；
- NPC 和世界不以主角意志为命令；
- 主角只有玩家输入权，没有更高的因果优先级或成功优先级；
- 同一地图内的 NPC 可以彼此自由互动，不必围绕主角行动；
- 所有本轮被程序分配到地图的启用 NPC 都参与推演，即使玩家不在其所在地点；
- 因地图容量不足而进入离线状态的 NPC 本轮不参与地点推演、属性分析或记忆摘要；
- 玩家意图涉及不在主角地图的角色时仍允许提交，但只能表现为寻找失败、无法直接接触或进行相关准备，不得生成双方直接互动；只要整轮成功，仍正常消耗一个时间段；
- AI 可以让行动失败、被拒绝、产生意外结果或引发其他角色行为，但必须遵守世界事实和程序规则。

### 4.4 AI 决策，程序裁决

- AI 负责理解意图、生成故事、判断可能的状态变化并提交结构化变更意图；
- 程序负责权限、类型、上下限、余额、位置、时间和并发版本校验；
- AI 不能直接连接数据库或任意修改值；
- 所有状态变化必须通过注册的内部接口或受控 Skill 完成。

### 4.5 角色信息严格隔离

- 世界保存客观事实；
- 每个角色只知道自己亲历、感知、被告知或通过明确游戏规则获知的信息；
- 玩家看到的其他角色独立纪事不会自动进入主角或 NPC 的记忆；
- 为不同角色构造 AI 上下文时，必须过滤掉该角色无权得知的内容。

## 5. 角色系统

### 5.1 全局角色与世界状态

角色先在全局角色库中创建，再由世界引用。角色库保存不可变角色 ID、创建后不可变且全局唯一的姓名、基础人设、系统提示词、世界书、视觉资源和单层基础属性定义。

每个世界有且只有一个主角，并可有 3–20 个 NPC；典型使用规模为 3–10 个 NPC。世界为每个引用角色保存且只保存一条变化记录，包含角色类型、启用状态和单层世界变化值；另以 `(world_id, role_id)` 唯一的世界角色记忆记录保存该角色在该世界中的长期记忆。主角由玩家控制且不可移除；NPC 从全局角色库选择，可从世界中加入、停用或移除。

角色库基础配置与世界变化值实时合成最终角色：基础表决定现存字段和类型，数值世界值作为变化量，字符串、布尔和枚举世界值作为替换值。世界变化缺少字段时使用基础值，只存在于世界变化中的遗留字段或类型不匹配值直接忽略。AI 只接收合成后的单层完整角色，不接收计算过程。

被任何世界引用的角色禁止从角色库删除，并显示引用它的世界。世界角色页只读展示最终属性；基础值、约束和更新规则只能在角色库修改，世界变化值只能由成功轮次的受控属性更新流程写入。

### 5.2 视觉资产

- 角色资源目录为 `content/characters/{角色名称}/`，文件夹名必须等于全局唯一角色名称；
- 默认立绘文件主体必须等于角色名称，其他同目录图片以描述作为文件主体，预留给未来表情差分或场景 CG；
- 同名资源按 JPG、JPEG、PNG、GIF、WebP、AVIF、BMP 的固定顺序取第一张；SVG 因可能包含脚本或外部资源而不支持；
- 默认立绘放入 1:1 相框，使用 contain 完整展示，不裁切、不拉伸；不同像素和宽高比均由展示层自适应；
- 未来场景 CG 使用全屏 contain，隐藏地图背景、角色立绘和其他图片层；第一版只保留资源描述和展示模式，不实现 AI 选图；
- 图片不进入 SQLite，升级和启动不得覆盖用户已有的同名资源。

### 5.3 NPC 位置规则

开发者预先定义地图和可用位置点。用户为每个 NPC 配置：

- 一个或多个候选位置；
- 星期条件；
- 晨间、午间、傍晚、夜晚条件；
- 指定时间段的固定位置；
- 在候选位置中随机出现；
- 仅有一个候选位置时形成固定位置 NPC。

每轮开始时，程序先依据规则确定角色位置，再将位置事实提供给 AI。AI 只能读取位置，不能改变任何角色的位置。地点推演始终使用本轮开始时冻结的位置；只有整轮成功后才推进时间并为下一时间段重新计算 NPC 位置。主角位置只由玩家移动决定。

每个可进入地点最多容纳 6 个角色，并永久为主角预留 1 个位置，因此每张地图最多分配 5 名 NPC。主角可以随时进入任意地图，不会挤走 NPC，也不会导致 NPC 突然换图。

NPC 首次分配严格遵循用户配置的位置规则。超出目标地图容量的 NPC 进入二次缓存区；全部地图完成首次分配后，程序以由世界、日期和时间段等冻结快照派生的可复现随机种子打乱缓存区，再按稳定地图顺序向未满地图逐个补充 NPC。所有地图达到 5 名 NPC 后仍剩余的角色进入明确的离线状态，本轮不属于任何地图。空缓存、全部满员、没有可用地图和离线角色都必须作为正常状态处理，不能依赖空值表达或引发空指针异常。

## 6. 第一版地图与场景

### 6.1 地图清单

第一版固定提供 7 张开发者制作的地图：

1. 总地图；
2. 家；
3. 地下城；
4. 商店街；
5. 冒险者工会；
6. 旅店；
7. 学校。

总地图是地点选择与导航页面，其余 6 张是可进入的地点地图。用户不能新增、删除或编辑地图定义。

### 6.2 总地图交互

总地图上显示一个代表主角的小型人物立绘，并为 6 个可进入地点设置预定义位置。

- 按键盘 Left/Right，在相邻地点之间移动主角；
- 移动过程使用简单行走动画，形成“小人走在地图上”的视觉感受；
- 鼠标第一次点击某个地点时，主角移动或选中该地点；
- 当主角已经位于目标地点时，再次点击该地点或按 Enter，进入对应地点地图；
- 只在总地图中移动、选择地点或进入地点都不算一次故事行动，也不推进世界时间。

### 6.3 地点地图交互

- 进入“家、地下城、商店街、冒险者工会、旅店、学校”后，浏览器背景切换为对应地点地图；
- 地图中使用单行 1:1 相框从左到右显示当前地点内最多 6 个角色；
- 玩家可以移动主角、靠近目标 NPC，或只停留在同一地点；
- 玩家提交文字意图后才触发本轮世界推演；
- 每张地点地图左上角提供浮动“地图”按钮，点击后返回总地图；
- 返回总地图不推进时间。

第一版占位地图只要求基础边界、角色显示、移动和地点进入/返回。依赖正式背景画面的精确障碍、碰撞区和交互点，在替换真实图片并确定布局后再配置。

### 6.4 16:9 背景与浮动 UI

- 每张地图背景统一为横版 16:9；
- 第一版设计基准为 1920×1080；
- 地图图片作为整个游戏浏览器视口的背景层；
- 时间、角色、故事、行动输入、进度和返回地图按钮均作为浮动 UI 叠加在背景上；
- 在 16:9 全屏显示时，背景应完整贴合视口；
- 非 16:9 视口保持图片比例，并以居中裁切方式填满，关键内容和交互位置必须放在 16:9 安全区域内。

### 6.5 占位图片与替换约定

第一版工程从仓库 `resource` 复制 7 张横向 16:9 的默认地图；图片像素尺寸可变，支持同名 JPG 或 PNG：

- 总地图；
- 家；
- 地下城；
- 商店街；
- 冒险者工会；
- 旅店；
- 学校。

中文文件主体是稳定资源契约，manifest 不保存扩展名。程序先查找 `.jpg`，再查找 `.png`；异常地同时存在时使用 JPG。后续只要使用同名、横向 16:9 的 JPG 或 PNG 替换对应文件，并刷新浏览器或重启应用，即可更新地图背景，不需要修改代码。地图锚点和碰撞配置使用归一化坐标，不能绑定某张图片的绝对像素。

## 7. 属性系统

### 7.1 属性定义

每个角色在全局角色库中直接配置自己的单层基础属性。支持整数、浮点数、文本、布尔和枚举，不再提供世界内属性模板或用户可编辑的世界覆盖表。

每个属性至少包含：

- 创建后不可修改的稳定键和可读显示名称；
- 数据类型和基础值；
- 含义说明；
- 允许操作；
- 更新规则；
- 可选数值上下限或枚举选项；
- 可选的应更新与不应更新短例子。

### 7.2 通用操作与合成

- 数值支持增加、减少和替换；世界记录保存相对基础值的变化量；
- 文本、布尔和枚举只支持替换；世界记录保存覆盖值；
- 替换数值时，程序根据目标最终值和当前基础值反算变化量；
- 角色主表新增属性会立即出现在所有引用世界，删除属性会让遗留世界变化值停止参与计算；
- 世界变化字段集合与基础字段集合不一致时不报业务错误，始终以基础字段集合为主。

### 7.3 更新规则与受控 Skill

“更新规则”指导属性分析 AI 客观判断角色本轮内容是否触发变化、使用何种操作，以及如何确定变化量或新值。普通的无互动描述通常不会触发，但只要本轮内容确实符合角色自己的更新规则，产生更新就是合理结果；产品不再额外细分或禁止这类语义情况。

属性与记忆分析任务按地图调用一次，同时分析该地图内全部角色。任务为每名角色读取其本轮纪事、可知客观事件、当前最终属性和对应更新规则，并在一份结构化结果中返回零个或多个属性更新意图以及一段长期记忆新增摘要。结算器根据不同字段分别调用属性更新能力和记忆追加能力，更新不同表，但两者仍属于同一轮原子提交。离线角色不运行属性与记忆分析。

Skill 只收集结构化结果，不直接写数据库，也不为动态属性生成或执行脚本。程序不替 AI 重做更新规则的语义判断，只校验角色、属性、操作、类型、约束和版本；属性更新不强制关联客观事件。任一意图或记忆结果无效时，该地图分析节点整体失败，不得部分写入或推进时间。

## 8. 一轮世界推演

### 8.1 玩家操作

1. 玩家在地图中移动到目标地点；
2. 玩家可以选择靠近某个 NPC，也可以只停留在该地点；
3. 玩家输入主角本轮意图；
4. 玩家提交后开始一次世界推演。

### 8.2 推演流程

1. 系统校验全应用没有其他活动轮次，并校验两个固定 AI 任务已经完成应用级配置；
2. 程序计算并冻结时间、所有角色位置、属性、角色配置、长期记忆、最近 5 个有效轮次、玩家意图和 AI 任务配置；
3. 每个有角色的地图启动一条独立链路，不创建离线角色链路；
4. 每条地图链先运行一次地点推演，且只能读取该地图角色；主角意图只进入主角所在地图；
5. 地点推演一次性返回该地图的互动分组、客观事件以及全部角色的独立纪事；
6. 该地图地点推演成功后立刻运行一次属性与记忆分析，同时返回所有地图内角色的属性更新意图和本轮记忆摘要；
7. 所有地图链并行运行，互不等待；
8. 程序汇总并校验全部地图结果；
9. 只有全部链路成功，才在一个原子事务中保存本轮、更新属性与记忆、推进时间并计算下一时间段 NPC 位置。

每个地点推演必须把本地图角色划分为一个或多个互动组：互动优先但不强迫，AI 可以选择全体互动、多个小组、两两互动或独处。每名本地图角色必须且只能属于一个组，组内可以包含连续动作和多轮对话，但角色不能在同一轮跨组参加另一事件。程序拒绝角色遗漏、重复、跨地图参与和未知角色。

地图链是第一版唯一工作流。系统固定提供 `location_simulation` 与 `attribute_memory_analysis` 两个任务类型，不开放任意任务、DAG、循环或脚本。调用上下文按地图和角色最小化；地点推演不读取其他地图，属性与记忆分析只读取本地图本轮结果、角色属性规则和记忆所需内容。

每个失败节点首次调用后最多再自动重试 2 次，即最多尝试 3 次。只重试失败节点，属性与记忆分析失败不得重新调用已经成功的地点推演。超时、临时网络错误、限流、Provider 服务端错误以及 JSON、Schema、分组或事件结构无效可以重试；用户取消、缺少模型、Provider/API Key 配置错误、认证或权限错误不重试，并立即给出可操作提示。任一节点耗尽重试后整轮失败。

### 8.3 性能与等待体验

- 典型 10 个 NPC 时，允许一轮耗时 1–3 分钟；
- 页面必须按地图显示当前节点、尝试次数、自动重试状态，并显示完成地图数、总地图数和总耗时；
- 用户可以取消；
- 当前页面仍打开时可以保留原意图重新提交；
- 失败、取消、连接中断、刷新或应用退出均不得生成正式轮次、推进时间或部分写入世界状态；
- 刷新或重启不恢复未完成轮次，只回到上一成功轮次。

## 9. 故事与记忆

### 9.1 每轮故事结构

每轮至少包含：

1. 主角独立纪事：只描述主角实际看见、听见、感受到或经历的内容，并固定第一个展示；
2. 其他角色独立纪事：按角色逐一展示各自视角，与主角纪事没有语义优先级差异；
3. 客观事件记录：记录谁在何时何地做了什么以及参与者；
4. 状态变更记录：记录经程序执行的属性和世界变化。

同一事件可以关联多个角色视角，但每段叙述应体现各自感受、认知和关注点，避免机械重复。

所有启用角色都出现在纪事列表中。有互动或其他实际经历的角色显示独立纪事；只有自己一人的地图角色只生成一句极短纪事，避免为凑篇幅虚构无意义剧情；离线角色显示由程序生成的固定简短离线说明。

### 9.2 历史保存

- 所有轮次故事永久保存在 SQLite；
- 每轮只进行一次逻辑结算和数据库提交；
- 游戏主界面以轮次卡片连续显示完整历史，旧轮次折叠、最新轮次展开，不建设独立历史页、筛选、分页、角色标签或分支控制；
- 第一版向 AI 提供角色长期记忆以及最近 5 个有效轮次中该角色有权知道的原文。近期原文与长期记忆允许重复，以加强 AI 对近期经历的印象。

“角色有效轮次”指该角色本轮被分配到某张地图并获得 AI 生成的独立纪事；离线角色的程序固定说明不计入最近 5 个有效轮次，独处角色的极短 AI 纪事计入。

### 9.3 记忆演进

每个世界中的每个角色拥有一条独立长期记忆记录，以 `(world_id, role_id)` 唯一。记录包含长文本 `memory` 字段。属性与记忆分析为本地图每名角色生成一段本轮新增摘要；第一次直接写入，后续以“原记忆 + `__` + 新摘要”的方式追加。`__` 是保留分隔符，单轮摘要本身不得包含它。

每角色每轮摘要推荐目标默认 20 字、硬上限默认 50 字；两个值都必须入库，并在统一的应用级 AI 任务配置页动态修改。超过当前硬上限视为结构无效并重试该地图分析节点。

当前版本不重写、不去重、不压缩长期记忆，不设置累计长度上限，也不在超出模型上下文前裁剪、警告或阻断。长期记忆整合、去重和重点强化留待后续独立设计。

永久不引入：

- 向量数据库；
- Embedding；
- 语义检索；
- RAG。

角色上下文只读取该角色自己的长期记忆、最近 5 个有效轮次中有权知道的内容、明确被告知的事实和世界公开事实。玩家可以阅读所有角色纪事，但其他角色纪事绝不因此进入该角色记忆。

## 10. 世界、保存与分支

### 10.1 独立世界

- 一个“存档”就是一个独立世界和独立 AI 会话上下文；
- Provider、API Key、模型目录、通用应用设置、两个固定 AI 任务配置和角色主表全局共享；
- 世界角色引用、变化值、位置规则、时间、故事和记忆属于各自世界；
- 新建世界不会读取旧世界的故事或角色状态。

### 10.2 自动保存

- 每轮成功后自动保存；
- 不依赖用户手动点击保存；
- 未完成、失败或取消的轮次不写入正式游戏数据；
- 页面关闭或应用重启后，可从最近一次成功轮次继续。

### 10.3 延后的快照与分支

当前版本只保留阶段 3 已存在的内部初始分支作为实现细节，不提供命名快照、存档点、分支创建、恢复、切换、删除或相关 API/UI，也不在本轮迁移角色变化状态。未来真正开发分支前必须重新设计并迁移所有分支拥有的状态，不能让多个可切换分支共享角色变化值或长期记忆。

## 11. 页面信息架构

### 11.1 世界列表

- 新建、进入和删除世界，名称固定显示为“世界 {id}”，不提供重命名；
- 新建世界固定从星期一、Day 1 开始，只能从全局角色库选择一个已有角色作为唯一主角；
- 原子创建世界、内部初始分支和引用已有角色的世界主角变化记录，不创建或修改全局角色主记录；
- 显示当前天数、时间段、主角、NPC 数量和最后游玩时间。

### 11.2 游戏主界面

- Phaser 2D 地图；
- 当前世界、天数、时间段和地点；
- 主角意图输入；
- 按地图展示的轮次节点进度、尝试次数、取消和错误提示；
- 按轮次排列的可折叠故事卡片；
- 卡片内依次展示主角独立纪事、其他角色独立纪事、客观事件和属性变化；
- 旧轮次默认折叠，最新成功轮次展开并滚动到结果。

### 11.3 角色管理

- 全局角色库负责角色 CRUD、基础人设、提示词、世界书、默认立绘和动态基础属性；
- 角色名称全局唯一且创建后不可修改；被世界引用时禁止删除并显示引用世界；
- 世界角色页只读展示主角与 NPC 的最终属性，不允许编辑世界变化值；
- NPC 从角色库加入世界并配置启用状态和位置规则；
- 主角置顶且不可移除，同一角色不能重复加入同一世界。

### 11.4 动态属性

- 在角色库为每个角色配置稳定键、类型、基础值、含义、约束、允许操作和更新规则；
- 角色主表变化会实时影响所有引用世界的最终属性字段集合和基础值；
- 世界变化值由受控 AI 更新意图和通用更新函数维护，用户界面不可直接编辑；
- 属性分析失败不得产生部分更新。

### 11.5 故事历史

不建设独立时间线页面。全部成功轮次直接在游戏主界面按时间顺序显示，不提供快照、恢复、分支、筛选、分页或按角色切换。

### 11.6 AI 设置

全局：

- Provider、Base URL、明文 API Key；
- 模型及能力；
- DeepSeek 官方预设；
- OpenAI-compatible 自定义接入；
- 连接测试；
- 固定的地点推演任务配置；
- 固定的属性与记忆分析任务配置。

两个任务配置属于应用级开发商设置，所有世界和地图共用，玩家创建世界时不需要配置。每个任务独立选择模型并配置温度、最大输出 Tokens、思考强度、超时、额外提示词、结构化输出策略和任意 Provider 参数 JSON；属性与记忆任务还配置摘要目标字数和硬上限。缺少任一必需模型时拒绝启动轮次。

自由 Provider 参数使用专门的多行 JSON 编辑器，支持格式化和语法错误定位。根节点必须是对象；前后端都验证 JSON 格式，但不验证未知厂商字段的业务含义。自由字段不得与通用字段或程序拥有的模型、消息、认证、流式协议和结构化输出 Schema 字段重名。

配置保存后对所有世界的下一次新轮次立即生效；已经启动的轮次使用启动时冻结的配置。需要结构化结果时，所选模型支持原生 JSON/结构化输出就启用该能力，否则使用提示词约束；两条路径都执行相同 Schema 校验，且不自动更换模型或 fallback。

最近轮次数第一版固定为 5，后续增加设置页面。

## 12. 第一版范围

第一版必须包含：

- Windows 11 本地启动器与默认浏览器；
- 多世界；
- 一个不可移除、基础配置可在全局角色库编辑的主角；
- 全局角色库 CRUD 与世界 NPC 引用；
- 可替换角色立绘和未来差分、场景 CG 的资源契约；
- 动态基础属性、世界变化值合成、更新规则和受控更新意图；
- NPC 位置规则；
- 总地图与家、地下城、商店街、冒险者工会、旅店、学校 6 张地点地图；
- 7 张可用同名 JPG 或 PNG 替换的横向 16:9 背景，像素尺寸可变；
- 总地图的 Left/Right、鼠标选择、Enter 进入和地点地图返回；
- 2D 地图移动、碰撞、位置和场景切换；
- 离散时间循环；
- 代码定义的按地图并行链路执行器；
- DeepSeek 官方 API 与通用 OpenAI-compatible API；
- 应用级固定 AI 任务配置与自由 Provider 参数 JSON；
- 主角和其他角色各自独立的角色纪事以及客观事件；
- 地图级属性与记忆分析、属性受控更新和世界角色长期记忆表；
- 长期记忆与最近 5 个有效轮次上下文；
- 单线自动保存和主界面可折叠历轮故事；
- 按地图进度、取消、有限自动重试和未完成轮次原子丢弃。

## 13. 后续功能

明确允许后续增加：

- 可视化配置顺序阶段与阶段内并行任务；
- 最近 N 轮的设置页面；
- 长期记忆整合、去重、压缩和重点强化；
- 命名快照、存档点和分支时间线；
- 根据角色状态选择表情差分立绘；
- 根据剧情事件选择并全屏展示角色绑定场景 CG；
- 更多开发者制作的地图、位置和视觉资产；
- 更多 AI Provider 专有适配。

## 14. 永久非目标

- 用户制作或上传地图、剧情、美术和 2D 资产；
- 云端账号、登录、权限和多人游戏；
- 局域网或公网服务；
- 微服务、Redis、Celery、消息队列和 Kubernetes；
- PostgreSQL；
- 向量数据库、Embedding 和 RAG；
- 任意 Python、JavaScript、Shell 或用户脚本执行；
- 完整 DAG 工作流引擎；
- 第三方多 Agent 编排框架；
- 复杂物理、3D 游戏和实时多人同步；
- Electron、Tauri、PySide6 或 pywebview 桌面壳。

## 15. 风险与产品约束

### 15.1 成本

所有被分配到地图的角色每轮都可能参与 AI 推演，调用量会随有角色地图数增长。产品通过按地图最小上下文、固定任务数量、模型人工选择、并行限制和过程可见控制成本，不会自动替用户选择更便宜的模型。当前版本按用户决策不限制或裁剪累计长期记忆，因此长时间游玩可能增加上下文与费用；该问题留给后续记忆整合功能。

### 15.2 一致性

生成式模型可能产生矛盾。空间、时间、角色知识和属性变更必须由结构化事实与程序校验兜底；自然语言故事不是数据库事实的唯一来源。

### 15.3 API Key

API Key 明文保存在 SQLite。这是明确接受的本地产品风险。应用必须避免通过页面查询、日志、异常、Prompt、故事或普通导出泄露完整 Key。

### 15.4 长期记忆增长

世界角色长期记忆使用长文本持续追加，并以 `__` 分隔每轮片段。当前版本不做累计长度限制、整合、去重、压缩、裁剪或归档；优先保证完整可玩闭环，后续再单独设计长期记忆治理。

### 15.5 真实浏览器验收

Playwright 只运行真实 AI 完整流程，不建设默认离线模拟 Playwright。测试会使用真实 Provider 和模型并产生费用。异常闭环不要求通过人为制造 Provider 故障进行端到端验证，而是必须额外完成一次针对错误分类、重试、取消和原子性的专项 code review；真实 Playwright 一旦自然遇到异常，就把该异常保留为回归案例，修复后用真实流程复验。

## 16. 第一版验收标准

1. Windows 11 x64 无需预装 Python 即可启动应用。
2. 应用只监听 127.0.0.1，并自动打开默认浏览器。
3. 用户可以配置 DeepSeek 官方或其他 OpenAI-compatible Provider 和模型。
4. 地点推演和属性与记忆分析两个应用级任务都必须明确选择模型，并能独立配置通用参数、额外提示词、结构化输出策略和自由 Provider JSON；玩家创建世界时无需配置。
5. 用户可以创建多个相互隔离的世界。
6. 每个世界有且只有一个不可移除的主角；世界创建固定为星期一 Day 1，名称自动显示为“世界 {id}”，并且只引用已经存在的全局角色，不创建或修改角色主记录。
7. 用户可以在全局角色库创建至少 20 个唯一角色，并从中向世界加入 NPC；常见 3–10 个 NPC 时操作流畅。
8. 角色可以配置基础人设、世界书、动态属性、更新规则和同名默认立绘；被世界引用时不能删除。
9. 总地图可以通过 Left/Right、鼠标和 Enter 完成地点选择与进入。
10. 6 张地点地图均可进入，并可通过左上角“地图”按钮返回总地图。
11. 7 张地图均为横向 16:9，像素尺寸可变，并能通过同名 JPG 或 PNG 替换；同名冲突时优先 JPG。
12. 地图背景按 cover 适配；角色立绘在 1:1 相框中按 contain 展示，不裁切、不拉伸。
13. 每个地点最多容纳 6 个角色并永久为主角预留一位，因此最多分配 5 名 NPC；溢出缓存按可复现随机顺序填补其他未满地图，最终仍无法分配的 NPC 进入明确离线状态且不会引发空指针异常。
14. 地图移动不推进时间；提交主角意图才触发一轮。
15. 四段时间、天数和星期按成功轮次正确推进。
16. 只有同地点角色才能直接互动。
17. 玩家意图始终按主角尝试处理；目标角色不在本地图时仍可提交，但不得生成双方直接互动，可以合理失败或准备并正常消耗成功轮次。
18. 同地图角色由 AI 合理划分互动组，每名角色恰好属于一个组；NPC 可以彼此互动且不必围绕主角，程序拒绝遗漏、重复和跨地图参与。
19. 所有有角色地图各自运行“地点推演 → 属性与记忆分析”链路并彼此并行；主角意图只进入主角地图。
20. 所有启用角色都出现在纪事列表中；主角只因展示顺序位于第一，独处角色使用极短纪事，离线角色使用固定简短说明。
21. 每个角色上下文只包含其亲历、感知、被告知、公开事实、自己的长期记忆和最近 5 个有效轮次可知原文，其他角色纪事不得串入。
22. 属性与记忆分析按地图一次处理所有地图内角色；属性语义由 AI 按角色规则判断，程序只校验结构、角色、属性键、操作、类型、范围和版本。
23. 角色主表与世界变化值按主表优先规则正确合成，属性替换、增加、减少、类型和上下限校验正确。
24. 每个世界角色拥有唯一记忆记录；本轮摘要默认目标 20 字、硬上限 50 字且可全局配置，成功时以 `__` 追加到长文本记忆，属性表和记忆表在同一事务中更新。
25. 所有成功轮次故事和状态均保存到数据库，并在游戏主界面显示为轮次卡片；旧轮次折叠、最新轮次展开，不提供独立历史页、快照或分支控制。
26. 每个失败节点最多尝试 3 次，只重试失败节点；可重试和不可重试错误分类符合产品定义。
27. 页面按地图显示当前节点、尝试次数、自动重试状态、完成数量和耗时，并允许取消。
28. 任一地图失败、取消、连接中断、刷新或应用退出都不会生成正式轮次、属性或记忆更新，也不会推进时间；重启只回到上一成功轮次。
29. 全局 AI 任务配置保存后对下一次新轮次生效，运行中轮次使用冻结配置，不自动选模或模型 fallback。
30. 世界间角色变化、位置规则、故事和长期记忆不会串线。
31. API Key 不出现在普通查询响应、日志、异常、Prompt、故事或普通历史中。
32. 从空数据库执行全部迁移后应用可正常运行；记忆唯一约束、属性与记忆原子提交以及未完成轮次无脏数据均有后端验证。
33. 错误分类、最多三次尝试、取消、连接中断和原子失败路径完成专项 code review；真实 Playwright 自然遇到的异常必须转成回归案例并复验。
34. 真实 AI Playwright 必须通过网页先创建角色，再使用该既有角色创建世界，并断言角色总数、ID 和内容不变，永久防止世界创建重复插入角色导致唯一键冲突。
35. 真实 AI Playwright 必须通过网页配置 Provider、模型和两个全局任务，创建属性与位置规则，移动主角，提交至少两轮自由意图，并验证地图范围、进度、角色纪事、客观事件、属性变化、`__` 记忆追加、时间推进、下一时段位置以及新旧故事卡片状态。
36. Playwright 不建设默认离线模拟流程；真实测试会产生费用，无可用凭据时必须明确报告“真实 AI 浏览器验收未验证”，不得以 mock 代替通过结论。
37. `天.jpg`、`莫莉莉.png`、`安可儿.png` 只作为隔离测试素材，不创建产品种子数据。
38. Windows onedir 包在干净 Windows 11 x64 环境完成启动和真实可玩流程冒烟，才可宣布合并阶段完成。
