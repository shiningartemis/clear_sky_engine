# AI 角色扮演生活模拟游戏：需求与产品设计

> 文档状态：经需求核对后的产品基线
> 日期：2026-07-11
> 适用范围：第一版产品及已明确的后续演进
> 本文回答“做什么、为谁做、如何使用、什么算完成”；技术实现依据见《技术选型文档》。

## 1. 产品定义

本产品是一款运行于 Windows 11 的单机 AI 角色扮演与生活模拟游戏。

游戏使用开发者制作的 2D 地图、美术资产和交互内容，为 AI 角色提供统一、可验证的时间与空间。角色只有处于同一地点时才能直接相遇和互动，从而解决传统纯文字角色扮演中常见的空间位置不分、时间概念薄弱、角色无故获得场外信息等问题。

产品不是通用聊天工具，也不是地图、剧情或美术创作工具。用户配置的是 AI 角色扮演所需的文字内容、属性、位置规则和模型；地图、剧情框架、角色视觉资产和全部 2D 游戏内容由开发者制作并随游戏交付。

## 2. 产品目标

第一版必须实现以下结果：

1. 玩家可以在开发者制作的 2D 地图中控制主角移动。
2. 玩家到达某个地点后，可以输入主角本轮意图，触发一次完整世界推演。
3. 主角和 NPC 根据各自人设、属性、关系、位置和记忆自主行动，而不是服从主角的命令。
4. 所有角色都受到同一时间、空间和客观世界事实约束。
5. 每轮生成主角视角故事与全局角色纪事，并可靠更新角色和世界状态。
6. 用户可以配置角色、属性、NPC 位置规则、Provider 和模型。
7. 支持多个相互隔离的世界、自动保存、命名快照和分支时间线。
8. 在 Windows 11 上以一个本地后台进程运行，并自动打开系统默认浏览器。

## 3. 目标用户与使用前提

目标用户是熟悉 AI 角色扮演、愿意配置人设和提示词，并希望获得具有真实时间与空间约束的动态生活模拟体验的单机玩家。

使用前提：

- 用户自行准备 DeepSeek 或 OpenAI-compatible API；
- 用户理解 AI 生成内容存在随机性；
- 用户接受单轮世界推演可能耗时 1–3 分钟；
- 用户接受 API Key 明文保存在本地 SQLite 的产品风险；
- 应用不提供云端账号、多人协作或联网存档。

## 4. 核心产品原则

### 4.1 空间是真实约束

- 地图位置是世界事实，不是故事中的自由文本。
- 只有物理意义上位于同一地点的角色才能直接见面、交谈或共同经历事件。
- AI 不得通过叙事让角色无条件跨越位置限制。

### 4.2 时间是离散回合

世界时间固定为四个时间段：

    晨间 → 午间 → 傍晚 → 夜晚 → 次日晨间

- 晨间对应约 08:00–11:00；
- 午间对应约 11:00–17:30，不再拆分上午、中午和下午；
- 傍晚对应约 17:30 至夜幕降临前；
- 夜晚对应约 19:30–23:59；
- 夜晚结束后天数加一，并进入次日晨间。

地图移动、查看页面和编辑设置不推进时间。只有玩家提交一次主角行为意图并成功生成、结算故事，才算一次行动并推进一个时间段。

### 4.3 角色拥有自主性

- 主角意图只代表玩家希望主角尝试做什么；
- NPC 根据自己的人设、属性、关系和记忆决定回应；
- NPC 和世界不以主角意志为命令；
- 所有启用的 NPC 每轮都参与世界推演，即使玩家不在其所在地点；
- 离屏 NPC 仍会移动、相遇、交谈和产生各自的故事；
- AI 可以让行动失败、被拒绝、产生意外结果或引发其他角色行为，但必须遵守世界事实和程序规则。

### 4.4 AI 决策，程序裁决

- AI 负责理解意图、生成故事、判断可能的状态变化并提交结构化变更意图；
- 程序负责权限、类型、上下限、余额、位置、时间和并发版本校验；
- AI 不能直接连接数据库或任意修改值；
- 所有状态变化必须通过注册的内部接口、Skill 或受控脚本完成。

### 4.5 角色信息严格隔离

- 世界保存客观事实；
- 每个角色只知道自己亲历、感知、被告知或通过明确游戏规则获知的信息；
- 玩家看到的全局角色纪事不会自动进入主角或 NPC 的记忆；
- 为不同角色构造 AI 上下文时，必须过滤掉该角色无权得知的内容。

## 5. 角色系统

### 5.1 统一角色模型

每个世界有且只有一个主角，并可有 3–20 个 NPC；典型使用规模为 3–10 个 NPC。

主角和 NPC 使用同一种角色模型，包含：

- 姓名；
- 人设；
- 系统提示词；
- 世界书；
- 绑定的游戏内置视觉资产；
- 属性集合；
- 当前状态；
- 角色独立记忆；
- 位置及位置规则。

区别仅为：

- 主角由玩家在地图中控制，且不可删除；
- NPC 由世界模拟控制，可由用户新增、编辑和删除。

主角虽然固定存在，但姓名、人设、提示词、世界书、属性和绑定资产都可以编辑。

### 5.2 视觉资产

- 头像、立绘、地图行走精灵和动画全部由开发者内置；
- 用户创建或编辑角色时，只能从资产目录中选择并绑定；
- 用户不能上传、生成或编辑 2D 资产；
- 资产使用稳定标识，版本升级时不得随意改变既有标识。

### 5.3 NPC 位置规则

开发者预先定义地图和可用位置点。用户为每个 NPC 配置：

- 一个或多个候选位置；
- 星期条件；
- 晨间、午间、傍晚、夜晚条件；
- 指定时间段的固定位置；
- 在候选位置中随机出现；
- 仅有一个候选位置时形成固定位置 NPC。

每轮开始时，程序先依据规则确定角色位置，再将位置事实提供给 AI。

## 6. 第一版地图与场景

### 6.1 地图清单

第一版固定提供 7 张开发者制作的地图：

1. 总地图；
2. 家；
3. 公园；
4. 商场；
5. 酒吧；
6. 情侣酒店；
7. 学校。

总地图是地点选择与导航页面，其余 6 张是可进入的地点地图。用户不能新增、删除或编辑地图定义。

### 6.2 总地图交互

总地图上显示一个代表主角的小型人物立绘，并为 6 个可进入地点设置预定义位置。

- 按键盘 Left/Right，在相邻地点之间移动主角；
- 移动过程使用简单行走动画，形成“小人走在地图上”的视觉感受；
- 鼠标第一次点击某个地点时，主角移动或选中该地点；
- 当主角已经位于目标地点时，再次点击该地点或按 Enter，进入对应地点地图；
- 只在总地图中移动、选择地点或进入地点都不算一次故事行动，也不推进世界时间。

### 6.3 地点地图交互

- 进入“家、公园、商场、酒吧、情侣酒店、学校”后，浏览器背景切换为对应地点地图；
- 地图中显示当前地点内的主角和 NPC；
- 玩家可以移动主角、靠近目标 NPC，或只停留在同一地点；
- 玩家提交文字意图后才触发本轮世界推演；
- 每张地点地图左上角提供浮动“地图”按钮，点击后返回总地图；
- 返回总地图不推进时间。

第一版占位地图只要求基础边界、角色显示、移动和地点进入/返回。依赖正式背景画面的精确障碍、碰撞区和交互点，在替换真实图片并确定布局后再配置。

### 6.4 16:9 背景与浮动 UI

- 每张地图背景统一为横版 16:9；
- 第一版设计基准为 1920×1080；
- 地图图片作为整个游戏浏览器视口的背景层；
- 时间、角色、故事、行动输入、进度和返回地图按钮均作为浮动 UI 叠加在背景上；
- 在 16:9 全屏显示时，背景应完整贴合视口；
- 非 16:9 视口保持图片比例，并以居中裁切方式填满，关键内容和交互位置必须放在 16:9 安全区域内。

### 6.5 占位图片与替换约定

第一版工程创建 7 张 1920×1080 的空白 JPG 占位图：

- 总地图.jpg；
- 家.jpg；
- 公园.jpg；
- 商场.jpg；
- 酒吧.jpg；
- 情侣酒店.jpg；
- 学校.jpg。

图片文件名是稳定资源契约。后续只要使用同名、同为 16:9 的 JPG 替换对应文件，并刷新浏览器或重启应用，即可更新地图背景，不需要修改代码。

## 7. 属性系统

### 7.1 属性类型

支持：

- 数值；
- 文本；
- 布尔；
- 枚举。

每个属性包含：

- 显示名称与稳定键；
- 类型；
- 初始值与当前值；
- 可选最小值和最大值；
- 说明；
- 允许操作；
- 更新规则。

### 7.2 属性模板与角色覆盖

- 一个世界可以定义可复用的属性模板；
- 用户为角色选择需要的模板；
- 角色可以覆盖自己的初始值、上下限和更新规则；
- 角色可以拥有不属于任何模板的独有属性；
- 修改模板时不能静默覆盖已经存在的角色覆盖项。

### 7.3 通用操作

所有可变属性默认支持：

- 替换：用于称号、头衔和其他文本或枚举值；
- 增加；
- 减少。

攻击、购买、关系变化等具有明确规则的行为，可以由开发者提供专用 Skill 或接口完成计算。

### 7.4 更新规则

“更新规则”是一段属性专用提示词，用于指导 AI 判断：

- 本轮故事是否触发该属性变化；
- 应使用替换、增加还是减少；
- 如何从故事中提取变化量或新值；
- 哪些情况不应更新；
- 判断所需的最小上下文。

AI 输出结构化变更意图，程序再次校验后才执行。例如购买行为应提交“金钱减少 5000”的命令，而不是直接写入最终数据库值。

## 8. 一轮世界推演

### 8.1 玩家操作

1. 玩家在地图中移动到目标地点；
2. 玩家可以选择靠近某个 NPC，也可以只停留在该地点；
3. 玩家输入主角本轮意图；
4. 玩家提交后开始一次世界推演。

### 8.2 推演流程

1. 系统校验当前世界没有其他运行中轮次；
2. 冻结时间、位置、属性、角色配置和最近故事组成的输入快照；
3. 运行代码定义的分阶段 AI 流水线；
4. 唯一且不可删除的主 AI 任务负责本轮主要世界推演；
5. 辅助 AI 任务按最小上下文补充独立角色故事或分析状态变化；
6. 汇总故事、客观事件和属性变更意图；
7. 程序校验并执行状态更新；
8. 在一个原子提交中保存本轮并推进时间。

第一版的具体阶段数和并行任务数在进入实现前单独确认。第一版使用代码定义任务，但内部执行器从一开始支持顺序阶段与阶段内并行任务。

每轮向主 AI 提供精简的世界索引、全部启用角色的核心配置、当前位置和当前属性值。详细属性更新规则通过渐进式披露交给对应分析任务，辅助任务只接收完成自身职责所需的最小上下文。

### 8.3 性能与等待体验

- 典型 10 个 NPC 时，允许一轮耗时 1–3 分钟；
- 页面必须显示当前阶段、并行任务、完成数量和耗时；
- 用户可以取消；
- 任务失败时可以安全重试；
- 失败、取消或中断均不得推进时间或部分写入世界状态。

## 9. 故事与记忆

### 9.1 每轮故事结构

每轮至少包含：

1. 主角视角：只描述主角实际看见、听见、感受到或经历的内容；
2. 全局角色纪事：按角色逐一展示每个角色的独立视角；
3. 客观事件记录：记录谁在何时何地做了什么以及参与者；
4. 状态变更记录：记录经程序执行的属性和世界变化。

同一事件可以关联多个角色视角，但每段叙述应体现各自感受、认知和关注点，避免机械重复。

### 9.2 历史保存

- 所有轮次故事永久保存在 SQLite；
- 每轮只进行一次逻辑结算和数据库提交；
- 玩家可以查看完整历史；
- 第一版发送给 AI 的故事上下文固定为最近 5 轮。

### 9.3 记忆演进

后续可以增加“每角色短期记忆总结子 Agent”，在每轮流水线中为各角色生成隔离的摘要。

永久不引入：

- 向量数据库；
- Embedding；
- 语义检索；
- RAG。

长期上下文问题只通过最近轮次、结构化摘要、明确过滤和渐进式披露解决。

## 10. 世界、保存与分支

### 10.1 独立世界

- 一个“存档”就是一个独立世界和独立 AI 会话上下文；
- Provider、API Key、模型目录和通用应用设置全局共享；
- 角色、属性、位置规则、时间、工作流选择、故事和记忆属于各自世界；
- 新建世界不会读取旧世界的故事或角色状态。

### 10.2 自动保存

- 每轮成功后自动保存；
- 不依赖用户手动点击保存；
- 失败轮次只保留运行诊断，不成为世界历史；
- 页面关闭或应用重启后，可从最近一次成功轮次继续。

### 10.3 命名快照与分支

- 用户可以为已成功轮次创建命名快照；
- 快照指向已有轮次，不重复复制全部历史；
- 从快照恢复会创建新分支；
- 原分支和其后续故事保持不变；
- 用户可以查看和切换不同分支；
- 分支删除必须明确确认。

## 11. 页面信息架构

### 11.1 世界列表

- 新建、重命名、进入和删除世界；
- 新建世界时选择起始星期，默认星期一；
- 显示当前天数、时间段、NPC 数量、当前分支和最后游玩时间。

### 11.2 游戏主界面

- Phaser 2D 地图；
- 当前世界、天数、时间段和地点；
- 主角意图输入；
- 轮次阶段进度、取消和重试；
- 主角视角故事；
- 全局角色纪事；
- 本轮属性变化摘要。

### 11.3 角色管理

- 主角与 NPC 列表；
- 人设、提示词、世界书和资产绑定；
- 属性模板、角色覆盖和独有属性；
- NPC 位置规则；
- 主角不可删除提示。

### 11.4 属性模板

- 模板 CRUD；
- 类型、默认值、上下限、说明和更新规则；
- 查看引用角色；
- 模板变更的同步确认。

### 11.5 时间线

- 轮次历史；
- 主角视角、角色纪事、客观事件和状态变化；
- 命名快照；
- 分支创建、查看、切换和删除。

### 11.6 AI 设置

全局：

- Provider、Base URL、明文 API Key；
- 模型及能力；
- DeepSeek 官方预设；
- OpenAI-compatible 自定义接入；
- 连接测试。

世界级：

- 第一版代码定义任务所使用的模型；
- 并发上限、超时和模型参数。

主 AI 任务始终唯一且不可删除；所有代码定义任务都必须人工选择模型后才能运行。

最近轮次数第一版固定为 5，后续增加设置页面。

## 12. 第一版范围

第一版必须包含：

- Windows 11 本地启动器与默认浏览器；
- 多世界；
- 一个不可删除但可编辑的主角；
- NPC CRUD；
- 内置视觉资产绑定；
- 属性模板、覆盖、独有属性和更新规则；
- NPC 位置规则；
- 总地图与家、公园、商场、酒吧、情侣酒店、学校 6 张地点地图；
- 7 张可同名替换的 1920×1080 JPG 占位背景；
- 总地图的 Left/Right、鼠标选择、Enter 进入和地点地图返回；
- 2D 地图移动、碰撞、位置和场景切换；
- 离散时间循环；
- 代码定义的通用阶段/并行任务执行器；
- DeepSeek 官方 API 与通用 OpenAI-compatible API；
- 主角视角、全局角色纪事和客观事件；
- 属性受控更新；
- 最近 5 轮上下文；
- 自动保存、命名快照和分支；
- 进度、取消、重试和中断恢复。

## 13. 后续功能

明确允许后续增加：

- 工作流配置持久化和 CRUD 页面；
- 可视化配置顺序阶段与阶段内并行任务；
- 最近 N 轮的设置页面；
- 每角色短期记忆总结子 Agent；
- 更多开发者制作的地图、位置和视觉资产；
- 更多 AI Provider 专有适配。

## 14. 永久非目标

- 用户制作或上传地图、剧情、美术和 2D 资产；
- 云端账号、登录、权限和多人游戏；
- 局域网或公网服务；
- 微服务、Redis、Celery、消息队列和 Kubernetes；
- PostgreSQL；
- 向量数据库、Embedding 和 RAG；
- 任意 Python、JavaScript、Shell 或用户脚本执行；
- 完整 DAG 工作流引擎；
- 第三方多 Agent 编排框架；
- 复杂物理、3D 游戏和实时多人同步；
- Electron、Tauri、PySide6 或 pywebview 桌面壳。

## 15. 风险与产品约束

### 15.1 成本

所有 NPC 每轮都可能参与 AI 推演，调用量会随 NPC 数和任务数增长。产品通过上下文裁剪、任务并发限制、模型人工选择和过程可见控制成本，不会自动替用户选择更便宜的模型。

### 15.2 一致性

生成式模型可能产生矛盾。空间、时间、角色知识和属性变更必须由结构化事实与程序校验兜底；自然语言故事不是数据库事实的唯一来源。

### 15.3 API Key

API Key 明文保存在 SQLite。这是明确接受的本地产品风险。应用必须避免通过页面查询、日志、异常、Prompt、故事或普通导出泄露完整 Key。

### 15.4 分支存储

每轮保存故事和状态快照会增加数据库体积。第一版优先保证可恢复与一致性，不提前实现复杂压缩或归档系统。

## 16. 第一版验收标准

1. Windows 11 x64 无需预装 Python 即可启动应用。
2. 应用只监听 127.0.0.1，并自动打开默认浏览器。
3. 用户可以配置 DeepSeek 官方或其他 OpenAI-compatible Provider 和模型。
4. 每个代码定义的 AI 任务都必须明确选择模型后才能运行。
5. 用户可以创建多个相互隔离的世界。
6. 每个世界有且只有一个不可删除但可编辑的主角。
7. 用户可以创建至少 20 个 NPC，常见 3–10 个时操作流畅。
8. 角色可以绑定内置资产并配置人设、世界书、属性和位置规则。
9. 总地图可以通过 Left/Right、鼠标和 Enter 完成地点选择与进入。
10. 6 张地点地图均可进入，并可通过左上角“地图”按钮返回总地图。
11. 7 张空白 JPG 均为 1920×1080、文件名正确，并能通过同名文件替换背景。
12. 地图背景在 16:9 全屏时完整贴合，所有 UI 浮动显示在背景之上。
13. 所有启用 NPC 每轮均参与推演，离屏角色也能移动、相遇和产生故事。
14. 地图移动不推进时间；提交主角意图才触发一轮。
15. 四段时间、天数和星期按成功轮次正确推进。
16. 只有同地点角色才能直接互动。
17. 主角视角不会泄露主角无法感知的信息。
18. 全局角色纪事包含所有活动角色各自的视角。
19. AI 只能提交状态变更意图，程序完成最终更新。
20. 属性替换、增加、减少、类型和上下限校验正确。
21. 所有成功轮次故事和状态均保存到数据库。
22. 最近 5 轮能够正确构造 AI 上下文。
23. 轮次过程可见，支持取消、失败重试和应用中断恢复。
24. 失败或取消不会产生半轮状态，也不会推进时间。
25. 命名快照可以创建新分支，原分支保持可访问。
26. 世界间数据不会串线。
27. API Key 不出现在普通查询响应、日志、异常或故事中。
28. 从空数据库执行全部迁移后应用可正常运行。
29. 后端、前端和关键端到端测试通过。
